# Production environment variables
# Используется при сборке production build (npm run build)

# Базовый URL для API-запросов
# В production режиме (когда frontend встроен в JAR) используется относительный путь
# Это позволяет избежать проблем с CORS и упрощает развертывание
VITE_API_BASE_URL=/api/v1
