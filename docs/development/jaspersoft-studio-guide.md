# Руководство по созданию отчётов в Jaspersoft Studio для FEMSQ

## 📋 Подготовка

### 1. Открыть проект в Jaspersoft Studio

1. Запустите **Jaspersoft Studio**
2. **File → Open Projects from File System...**
3. Выберите директорию: `/home/alex/projects/java/spring/vue/femsq/code/femsq-backend/femsq-reports/`
4. Нажмите **Finish**

### 2. Добавить JDBC драйвер MS SQL Server

**Перед созданием Data Adapter нужно добавить драйвер в Jaspersoft Studio.**

#### Вариант A: Автоматическая настройка (рекомендуется)

```bash
cd /home/alex/projects/java/spring/vue/femsq/code
./scripts/setup-jaspersoft-drivers.sh
```

Скрипт скопирует драйвер в `~/jaspersoft-drivers/mssql-jdbc.jar`.

#### Вариант B: Ручная настройка

Путь к драйверу в Maven репозитории:
```
/home/alex/.m2/repository/com/microsoft/sqlserver/mssql-jdbc/12.8.1.jre11/mssql-jdbc-12.8.1.jre11.jar
```

### 3. Настроить подключение к БД (для тестирования)

1. **Window → Show View → Repository Explorer**
2. Правой кнопкой на **Data Adapters** → **Create Data Adapter**
3. Выберите **Database JDBC Connection**
4. Заполните:
   - **Name:** `FEMSQ MS SQL Server`
   - **JDBC Driver:** `com.microsoft.sqlserver.jdbc.SQLServerDriver`
   - **JDBC URL:** `jdbc:sqlserver://localhost:1433;databaseName=FishEye;encrypt=false;trustServerCertificate=true`
   - **Username:** ваш пользователь БД
   - **Password:** ваш пароль
5. **ВАЖНО: Добавить драйвер в classpath:**
   - Нажмите кнопку **...** рядом с **JDBC Driver**
   - Вкладка **Drivers** → **Add**
   - Укажите путь к JAR:
     - Если использовали скрипт: `~/jaspersoft-drivers/mssql-jdbc.jar`
     - Или напрямую: `/home/alex/.m2/repository/com/microsoft/sqlserver/mssql-jdbc/12.8.1.jre11/mssql-jdbc-12.8.1.jre11.jar`
   - **Driver Class:** `com.microsoft.sqlserver.jdbc.SQLServerDriver`
   - **OK**
6. **Test Connection** → должно быть **Success**
7. Нажмите **Finish**

**Важно:** Это подключение только для разработки. В продакшене используется `ConnectionFactory` из приложения.

**Если ошибка:** `ClassNotFoundException: com.microsoft.sqlserver.jdbc.SQLServerDriver`
- Убедитесь, что драйвер добавлен в Data Adapter (шаг 5)
- Или добавьте JAR в classpath проекта: **Project → Properties → Java Build Path → Libraries → Add External JARs**

---

## 🆕 Создание нового отчёта

### Шаг 1: Создать новый JRXML файл

1. В **Project Explorer** перейдите в: `src/main/resources/reports/embedded/`
2. Правой кнопкой → **New → Jasper Report**
3. Выберите шаблон: **Blank A4** (или другой по необходимости)
4. **File name:** `my-new-report.jrxml`
5. **Next → Next → Finish**

### Шаг 2: Настроить параметры отчёта

В **Outline** (справа) найдите **Parameters** и добавьте обязательные:

#### Параметр `SCHEMA_NAME` (обязательный)

1. Правой кнопкой на **Parameters** → **Create Parameter**
2. Заполните:
   - **Name:** `SCHEMA_NAME`
   - **Class:** `java.lang.String`
   - **Default Value Expression:** `"ags"` (или `"ags_test"` для тестовой БД)
   - **Is For Prompting:** `false` ✅
3. **OK**

#### Параметры бизнес-логики (например, `contractorId`)

1. Правой кнопкой на **Parameters** → **Create Parameter**
2. Заполните:
   - **Name:** `contractorId` (или другой по необходимости)
   - **Class:** `java.lang.String` (или `java.lang.Integer`)
   - **Is For Prompting:** `false` ✅
3. **OK**

**Важно:** Все параметры должны иметь `isForPrompting="false"`, так как они передаются из приложения.

---

### Шаг 3: Настроить SQL-запрос

1. В **Designer** перейдите на вкладку **Design**
2. В **Outline** найдите **Report Query**
3. Двойной клик на **Report Query** → откроется редактор запросов
4. Выберите **Data Adapter:** `FEMSQ MS SQL Server` (созданный ранее)
5. Введите SQL-запрос с использованием параметра схемы:

```sql
SELECT 
  og.ogKey,
  og.ogNm AS ogName,
  og.ogNmFl AS fullName,
  og.ogRgTaxType AS ogType
FROM $P!{SCHEMA_NAME}.og og
WHERE og.ogKey = CAST($P{contractorId} AS INT)
```

**Ключевые моменты:**
- `$P!{SCHEMA_NAME}` — **прямая подстановка** схемы (не параметр запроса)
- `$P{contractorId}` — **параметр запроса** (передаётся в PreparedStatement)
- Используйте алиасы (`AS ogName`) для удобства в отчёте

6. Нажмите **Read Fields** → поля появятся в **Outline → Fields**
7. **OK**

---

### Шаг 4: Дизайн отчёта

#### 4.1. Заголовок (Title Band)

1. В **Outline** найдите **Title**
2. Перетащите **Static Text** из **Palette** (слева)
3. В свойствах:
   - **Text:** `МОЙ НОВЫЙ ОТЧЁТ`
   - **Font:** размер 18, Bold
   - **Alignment:** Center

#### 4.2. Детали (Detail Band)

1. В **Outline** найдите **Detail**
2. Перетащите элементы из **Palette:**
   - **Static Text** — для меток (например, "Код:")
   - **Text Field** — для данных (перетащите из **Fields** в **Outline**)

**Пример размещения полей:**

```
[Static Text: "Код:"] [Text Field: $F{ogKey}]
[Static Text: "Название:"] [Text Field: $F{ogName}]
```

3. Для каждого **Text Field**:
   - **Expression:** выберите поле из списка (например, `$F{ogName}`)
   - **Blank When Null:** `true` ✅ (или используйте тернарный оператор)

**Обработка NULL значений:**
```java
$F{ogName} != null ? $F{ogName} : ""
```

#### 4.3. Подвал (Page Footer)

1. В **Outline** найдите **Page Footer**
2. Добавьте **Text Field** с выражением:
```java
"Страница " + $V{PAGE_NUMBER} + " из " + $V{PAGE_NUMBER}
```

---

### Шаг 5: Тестирование отчёта

1. В **Outline** найдите **Parameters** → двойной клик
2. Установите тестовые значения:
   - `SCHEMA_NAME` = `"ags_test"` (или `"ags"`)
   - `contractorId` = `"518"` (или другой существующий ID)
3. **OK**
4. Нажмите **Preview** (кнопка с иконкой глаза) или **F5**
5. Отчёт должен отобразиться с данными из БД

**Если ошибка подключения:**
- Проверьте, что SQL Server запущен
- Проверьте параметры подключения в Data Adapter
- Убедитесь, что схема указана правильно

---

### Шаг 6: Экспорт в проект

1. **File → Save** (или **Ctrl+S**)
2. Файл сохранится в: `src/main/resources/reports/embedded/my-new-report.jrxml`

**Важно:** Jaspersoft Studio может добавить служебные файлы (`.jasper`, `.jrxml.bak`). Удалите их, оставьте только `.jrxml`.

---

## 📝 Создание JSON-метаданных

Для каждого отчёта нужен файл метаданных `.json` в той же директории.

### Пример: `my-new-report.json`

```json
{
  "id": "my-new-report",
  "name": "Мой новый отчёт",
  "description": "Описание отчёта",
  "category": "contractors",
  "tags": ["contractors", "custom"],
  "parameters": [
    {
      "name": "contractorId",
      "type": "string",
      "required": true,
      "description": "ID контрагента"
    }
  ]
}
```

**Создайте файл вручную** в той же директории, что и `.jrxml`.

---

## ✅ Контрольный список

Перед сохранением проверьте:

- [ ] Параметр `SCHEMA_NAME` добавлен с `defaultValueExpression="ags"`
- [ ] Все параметры имеют `isForPrompting="false"`
- [ ] SQL-запрос использует `$P!{SCHEMA_NAME}` для схемы
- [ ] Все поля обрабатывают NULL значения
- [ ] UUID элементов сгенерированы корректно (можно оставить автоматические)
- [ ] Отчёт протестирован в Preview с реальными данными
- [ ] Создан файл `.json` с метаданными
- [ ] Файл сохранён в `reports/embedded/`

---

## 🔧 Важные особенности FEMSQ

### 1. Динамическая схема БД

**Всегда используйте:**
```sql
FROM $P!{SCHEMA_NAME}.table_name
```

**НЕ используйте:**
```sql
FROM ags.table_name  -- ❌ Хардкод схемы
```

### 2. Параметры запросов

- `$P!{param}` — **прямая подстановка** (для схем, имён таблиц)
- `$P{param}` — **параметр PreparedStatement** (для значений WHERE)

### 3. Обработка NULL

**Всегда проверяйте NULL:**
```java
$F{fieldName} != null ? $F{fieldName} : ""
```

### 4. UUID элементов

Jaspersoft Studio автоматически генерирует UUID. Можно оставить автоматические, но они должны быть валидными RFC 4122 UUID.

---

## 🐛 Решение проблем

### Ошибка: "ClassNotFoundException: com.microsoft.sqlserver.jdbc.SQLServerDriver"

**Причина:** JDBC драйвер не добавлен в classpath Jaspersoft Studio.

**Решение:**

1. **Добавить драйвер в Data Adapter:**
   - **Repository Explorer → Data Adapters → Edit** (ваш адаптер)
   - Вкладка **Drivers → Add**
   - Укажите путь: `/home/alex/.m2/repository/com/microsoft/sqlserver/mssql-jdbc/12.8.1.jre11/mssql-jdbc-12.8.1.jre11.jar`
   - **Driver Class:** `com.microsoft.sqlserver.jdbc.SQLServerDriver`
   - **OK → Test Connection**

2. **Или добавить в classpath проекта:**
   - **Project Explorer → Properties → Java Build Path → Libraries**
   - **Add External JARs...**
   - Выберите: `/home/alex/.m2/repository/com/microsoft/sqlserver/mssql-jdbc/12.8.1.jre11/mssql-jdbc-12.8.1.jre11.jar`
   - **Apply and Close**

3. **Или использовать скрипт:**
   ```bash
   ./scripts/setup-jaspersoft-drivers.sh
   ```
   Затем добавьте `~/jaspersoft-drivers/mssql-jdbc.jar` в Data Adapter.

### Ошибка: "Invalid UUID string"

**Причина:** UUID в JRXML не соответствует формату RFC 4122.

**Решение:** Jaspersoft Studio генерирует корректные UUID автоматически. Если редактируете вручную, используйте формат: `xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx`

### Ошибка: "Недопустимое имя столбца"

**Причина:** Неправильные имена столбцов в SQL или несоответствие с полями.

**Решение:**
1. Проверьте реальные имена столбцов в БД
2. Используйте алиасы в SQL: `og.ogNm AS ogName`
3. Убедитесь, что поля в **Fields** соответствуют алиасам

### Ошибка: "Индекс находится вне диапазона"

**Причина:** Неправильное использование `$P!{}` и `$P{}` в SQL.

**Решение:**
- Для схем/таблиц: `$P!{SCHEMA_NAME}`
- Для значений: `$P{contractorId}`

### Отчёт пустой

**Причина:** Нет данных или неправильный WHERE.

**Решение:**
1. Проверьте SQL-запрос в Preview
2. Убедитесь, что параметры передаются корректно
3. Проверьте логи приложения на наличие ошибок SQL

---

## 📚 Примеры существующих отчётов

Изучите существующие отчёты как примеры:

- `contractor-card.jrxml` — простой отчёт с одной таблицей
- `contractor-with-objects.jrxml` — отчёт с подотчётом (subreport)
- `objects-for-contractor.jrxml` — подотчёт

**Расположение:** `code/femsq-backend/femsq-reports/src/main/resources/reports/embedded/`

---

## 🔗 Полезные ссылки

- [JasperReports Documentation](https://community.jaspersoft.com/documentation)
- [Jaspersoft Studio User Guide](https://community.jaspersoft.com/documentation/jaspersoft-studio-user-guide)
- Структура проекта FEMSQ: `docs/project-documentation-rules.md`

---

**Дата создания:** 2025-11-26  
**Версия:** 1.0  
**Автор:** Александр
