# Инструкция по запуску и проверке модуля Reports

## 📦 Сборка приложения

✅ **Приложение собрано и готово к запуску!**

JAR файл находится по пути:
```
/home/alex/projects/java/spring/vue/femsq/code/femsq-backend/femsq-web/target/femsq-web-0.1.0.1-SNAPSHOT.jar
```

**Важно:** Модуль Reports уже включён в сборку и будет доступен при запуске.

Если нужно пересобрать:
```bash
cd /home/alex/projects/java/spring/vue/femsq/code/femsq-backend/femsq-web
mvn package -DskipTests
```

## 🚀 Запуск приложения

### 1. Подготовка

Убедитесь, что:
- ✅ MS SQL Server запущен на `localhost:1433`
- ✅ База данных `FishEye` доступна
- ✅ Учётные данные для подключения настроены в `application.yml`

### 2. Запуск JAR файла

```bash
cd /home/alex/projects/java/spring/vue/femsq/code/femsq-backend/femsq-web/target
java -jar femsq-web-0.1.0.1-SNAPSHOT.jar
```

Или с указанием рабочей директории (для хранения внешних отчётов):
```bash
cd /home/alex/projects/java/spring/vue/femsq/code/femsq-backend/femsq-web/target
java -jar femsq-web-0.1.0.1-SNAPSHOT.jar --spring.config.location=classpath:/application.yml
```

### 3. Проверка запуска

После запуска приложение должно:
- ✅ Запуститься на порту (по умолчанию `8080`, проверьте `application.yml`)
- ✅ Создать директории `./reports/custom/` и `./reports/templates/` в рабочей директории
- ✅ Загрузить встроенные отчёты из `src/main/resources/reports/embedded/`

Проверьте логи на наличие:
```
INFO  - Started FemsqWebApplication
INFO  - ReportDiscoveryService: Scanning for reports...
INFO  - ReportDiscoveryService: Found X embedded reports
```

## ✅ Проверка функциональности

### Шаг 1: Проверка REST API - Список отчётов

Откройте в браузере или через curl:
```bash
curl http://localhost:8080/api/v1/reports/available
```

**Ожидаемый результат:**
```json
[
  {
    "id": "contractor-card",
    "name": "Карточка контрагента",
    "description": "Подробная информация о контрагенте",
    "category": "contractors",
    "tags": ["contractors", "card", "print"],
    "source": "embedded"
  },
  {
    "id": "objects-list",
    "name": "Список объектов",
    "description": "Список выбранных объектов",
    "category": "objects",
    "tags": ["objects", "list", "construction"],
    "source": "embedded"
  },
  {
    "id": "contractor-with-objects",
    "name": "Контрагент с объектами",
    "description": "Информация о контрагенте и его объектах",
    "category": "contractors",
    "tags": ["contractors", "objects", "subreport", "demo"],
    "source": "embedded"
  }
]
```

### Шаг 2: Проверка метаданных отчёта

```bash
curl http://localhost:8080/api/v1/reports/contractor-card/metadata
```

**Ожидаемый результат:** JSON с полными метаданными отчёта, включая параметры.

### Шаг 3: Проверка UI - Каталог отчётов

1. Откройте браузер: `http://localhost:8080`
2. Подключитесь к базе данных (если требуется)
3. Перейдите в раздел "Отчёты" (кнопка в верхней панели)
4. **Проверьте:**
   - ✅ Отображается каталог с 3 встроенными отчётами
   - ✅ Видны карточки отчётов с названиями и описаниями
   - ✅ Работают фильтры по категориям и тегам
   - ✅ Кнопка "Обновить список" работает

### Шаг 4: Проверка диалога параметров

1. В каталоге отчётов нажмите "Сгенерировать" на любом отчёте
2. **Проверьте:**
   - ✅ Открывается диалог с параметрами
   - ✅ Поля заполняются значениями по умолчанию (если есть)
   - ✅ Валидация работает (required поля)
   - ✅ Кнопки "Отмена", "Предпросмотр", "Сгенерировать" доступны

### Шаг 5: Тест генерации PDF

1. В диалоге параметров заполните обязательные поля:
   - Для `contractor-card`: `contractorId` (например, "1")
   - Для `objects-list`: `objectIds` (например, "1,2,3")
2. Выберите формат "PDF"
3. Нажмите "Сгенерировать"
4. **Проверьте:**
   - ✅ PDF файл скачивается
   - ✅ Файл открывается и содержит данные из БД
   - ✅ Форматирование корректное

### Шаг 6: Проверка автообнаружения внешних отчётов

1. Создайте тестовый отчёт в директории `./reports/custom/`:
   ```bash
   # Скопируйте один из встроенных отчётов как пример
   cp code/femsq-backend/femsq-reports/src/main/resources/reports/embedded/contractor-card.jrxml ./reports/custom/test-report.jrxml
   ```

2. Создайте метаданные `./reports/custom/test-report.json`:
   ```json
   {
     "id": "test-report",
     "version": "1.0.0",
     "name": "Тестовый отчёт",
     "description": "Отчёт для проверки автообнаружения",
     "category": "test",
     "tags": ["test", "custom"],
     "files": {
       "template": "test-report.jrxml"
     },
     "parameters": [
       {
         "name": "testParam",
         "type": "string",
         "label": "Тестовый параметр",
         "required": true
       }
     ]
   }
   ```

3. Подождите 60 секунд (интервал автообновления)
4. Проверьте через API:
   ```bash
   curl http://localhost:8080/api/v1/reports/available
   ```
5. **Проверьте:**
   - ✅ Новый отчёт появился в списке с `"source": "external"`
   - ✅ Отчёт доступен в UI каталоге

### Шаг 7: Тест hot-reload

1. Измените метаданные `./reports/custom/test-report.json`:
   - Измените `"name"` на другое значение
2. Подождите 60 секунд
3. Проверьте через API:
   ```bash
   curl http://localhost:8080/api/v1/reports/test-report/metadata
   ```
4. **Проверьте:**
   - ✅ Метаданные обновились без перезапуска приложения
   - ✅ Изменения отображаются в UI

### Шаг 8: Проверка контекстной генерации (опционально)

Если у вас есть компоненты `ContractorCard` или `ObjectsList`:
1. Откройте карточку контрагента
2. Нажмите кнопку "Отчёты"
3. **Проверьте:**
   - ✅ Появляется список отчётов, доступных для этого контекста
   - ✅ Параметры автоматически заполняются из контекста
   - ✅ Генерация работает с предзаполненными параметрами

## 🔍 Проверка логов

Во время работы приложения проверьте логи на наличие:

### Успешные операции:
```
INFO  - ReportDiscoveryService: Scanning for reports...
INFO  - ReportDiscoveryService: Found 3 embedded reports
INFO  - JasperReportsEngine: Compiled report: contractor-card
INFO  - ReportGenerationService: Generated report: contractor-card (PDF, 12345 bytes)
```

### Ошибки (если есть):
```
ERROR - ReportDiscoveryService: Failed to load report: test-report
ERROR - ReportGenerationService: Failed to generate report: contractor-card
```

## 📝 Чек-лист проверки

- [ ] Приложение запускается без ошибок
- [ ] Директории `./reports/custom/` и `./reports/templates/` созданы
- [ ] API `/api/v1/reports/available` возвращает список отчётов
- [ ] API `/api/v1/reports/{id}/metadata` возвращает метаданные
- [ ] UI каталог отчётов отображается корректно
- [ ] Диалог параметров работает
- [ ] Генерация PDF работает
- [ ] Внешние отчёты обнаруживаются автоматически
- [ ] Hot-reload работает (обновление без перезапуска)
- [ ] Логи не содержат критических ошибок

## 🐛 Устранение проблем

### Проблема: Отчёты не загружаются
**Решение:**
- Проверьте логи на ошибки загрузки
- Убедитесь, что файлы JRXML и JSON находятся в правильных директориях
- Проверьте формат JSON метаданных

### Проблема: Генерация PDF не работает
**Решение:**
- Проверьте подключение к БД
- Убедитесь, что SQL запросы в JRXML корректны
- Проверьте параметры отчёта (все required должны быть заполнены)

### Проблема: Hot-reload не работает
**Решение:**
- Убедитесь, что прошло 60 секунд после изменения файлов
- Проверьте права доступа к файлам в `./reports/custom/`
- Проверьте логи на ошибки сканирования

## 📚 Дополнительная информация

- Техническая документация: `docs/development/technical/reports-development-guide.md`
- ADR: `docs/project/decisions/adr-003-reporting-engine.md`
- Примеры отчётов: `code/femsq-backend/femsq-reports/src/main/resources/reports/embedded/`
