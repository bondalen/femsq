# План следующего шага: reconcile-specific по `af_type` (2/3/5/6)

**Дата создания:** 2026-03-23  
**Проект:** FEMSQ  
**Версия плана:** 0.9.1  

---

## Цель
Реализовать и верифицировать доменную логику reconcile для `af_type ∈ {2,3,5,6}`:
- заполнение доменных таблиц согласно логике VBA,
- запись изменений в соответствующие `*_change`-журналы,
- идемпотентность повторного запуска на том же `exec_key`,
- чек-листы верификации и финальная ручная проверка.

---

## Вход в чат (что уже сделано)
В предыдущем чате `chat-plan-26-0319-excel-processing-parallel.md` выполнены:
- общий Excel-инфраструктурный каркас (`audit.excel`) и Stage 1–2 (FK/derived) для типов 2/3/5/6,
- общий reconcile-каркас (контракты `ReconcileContext/ReconcileResult`, координатор, транзакционные границы на тип файла) — `Фаза 5.0`.

---

## Фаза 1: Type 5 (`ra_stg_ra` → `ags_ra` / `ags_ra_change`)
**Выполнить логику сверки и переноса данных VBA.**

### 1.1. Match-логика с доменными данными
- [ ] сопоставление staging-записей с существующими доменными (`ags_ra`)
- [ ] правила обработки неоднозначности/пустого соответствия

### 1.2. Upsert в `ags_ra`
- [ ] insert/update согласно правилам VBA `ra_aAllAgents.Audit()`
- [ ] учёт `addRa` и корректное заполнение счётчиков изменений

### 1.3. Запись изменений в `ags_ra_change`
- [ ] перенос/фиксация изменений в журнал `*_change`
- [ ] привязка к результатам reconcile текущего `exec_key`

### 1.4. Специфические операции из VBA
- [ ] реализовать оставшиеся операции сверки (`delete/close/reopen/...`) по результатам анализа VBA
- [ ] зафиксировать порядок выполнения

### 1.5. Идемпотентность type 5
- [ ] повторный запуск на одном `exec_key` (без дублей/потерь)
- [ ] поведение повторного запуска по той же ревизии

### 1.6. Верификация type 5
- [ ] чек-лист проверки результата
- [ ] сверка цепочки `ra_stg_ra` → `ags_ra` → `ags_ra_change` → journal выполнения

---

## Фаза 2: Type 2 (`ra_stg_cn_prdoc` → домен)
**Выполнить reconcile по VBA `RAAudit_cn_PrDoc`.**

- [ ] реализация reconcile (контур match/upsert/delete по результатам VBA)
- [ ] адаптация SQL-логики Access под источники staging
- [ ] запись изменений в соответствующие `*_change` (если предусмотрено моделью reconcile)

---

## Фаза 3: Type 3 (`ra_stg_ralp` + `ra_stg_ralp_sm` → домен)
**Выполнить reconcile по VBA `RAAudit_ralp` (оба листа).**

- [ ] reconcile на базе обеих staging-таблиц
- [ ] зафиксировать порядок применения изменений между `ra_stg_ralp` и `ra_stg_ralp_sm`
- [ ] запись изменений в `*_change` (если предусмотрено моделью reconcile)

---

## Фаза 4: Type 6 (`ra_stg_agfee` → домен)
**Выполнить reconcile по VBA `RAAudit_AgFee_Month` + `ra_aAgFee23_06.Audit`.**

- [ ] реализация reconcile logic для type 6
- [ ] сохранить guard по `auditType` (согласованный на стадии Stage 2)
- [ ] запись изменений в `*_change` (если предусмотрено моделью reconcile)

---

## Фаза 5: cross-type (идемпотентность / SQL artifacts / Liquibase)

- [ ] 5.1. Идемпотентность и корректность повторного запуска: правила upsert/update/delete для каждого типа
- [ ] 5.2. SQL-артефакты reconcile и Liquibase: оформить стабильные сценарии и управляемые изменения схемы/служебных объектов

---

## Фаза 6: Верификация и финальная ручная проверка
- [ ] чек-листы верификации для `2/3/5/6`
- [ ] сверка счётчиков: staging → доменные таблицы → journal выполнения
- [ ] финальный прогон `JAR` для `af_type=2,3,5,6` с ручной проверкой UI/логов
- [ ] проверка через DBHub:
  - [ ] staging-таблицы заполнены по правильному `*_exec_key`
  - [ ] при `af_source=false` staging не загружается (лог отражает счётчики)
  - [ ] при `af_source=true` и `addRa=true` доменные таблицы получают новые/обновлённые данные

---

## Параллелизм
- Type 5 можно закрывать независимо после готовности Stage 1–2 для type 5.
- Type 2/3/6 можно выполнять параллельно между собой, фиксируя порядок применения изменений только внутри своих композиций (например, Type 3 — два листа).

