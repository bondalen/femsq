# Анализ интеграции FilesList: связь один-ко-многим

**Дата:** 2026-01-19  
**Цель:** Проверить корректность реализации связи "ревизия → директория → файлы"

---

## Текущая реализация

### Цепь загрузки данных:

1. **Ревизия → Директория:**
   - При выборе ревизии (`selectedAuditId`) в `watch` вызывается `loadDirectory(auditId)`
   - `loadDirectory` использует `directoriesApi.getDirectoryByAuditId(auditId)`
   - Результат сохраняется в `currentDirectory.value` (тип `RaDirDto | null`)
   - `currentDirectory` содержит поле `key` (ID директории)

2. **Директория → Файлы:**
   - `FilesList` получает prop `:dir-id="currentDirectory?.key ?? null"`
   - Внутри `FilesList` есть `watch` на `props.dirId` с `immediate: true`
   - При изменении `dirId` вызывается `filesStore.loadByDirId(newDirId)`
   - Файлы загружаются через API `filesApi.getFilesByDirId(dirId)`
   - Computed `files` использует `filesStore.filesByDirId(props.dirId)` для получения файлов

---

## Анализ корректности

### ✅ Что работает правильно:

1. **Цепь загрузки:**
   - Ревизия → Директория: ✅ Реализовано через `loadDirectory(auditId)`
   - Директория → Файлы: ✅ Реализовано через `watch` в `FilesList` на `props.dirId`

2. **Автоматическая загрузка:**
   - `FilesList` имеет `watch` с `immediate: true`, что означает загрузку при монтировании
   - При изменении `dirId` файлы перезагружаются автоматически

3. **Связь один-ко-многим:**
   - Один `ra_a` (ревизия) → один `ra_dir` (директория) через `adt_dir`
   - Один `ra_dir` (директория) → много `ra_f` (файлы) через `af_dir = ra_dir.key`

---

## Потенциальные проблемы и предложения

### 1. ⚠️ Проблема: Асинхронная загрузка директории

**Текущая ситуация:**
- `currentDirectory` загружается асинхронно в `watch(selectedAuditId)`
- `FilesList` может получить `dirId = null` сначала, затем `key` после загрузки
- `watch` в `FilesList` должен сработать при изменении `dirId`, но есть риск race condition

**Предложение:**
- ✅ Текущая реализация должна работать, так как `watch` в `FilesList` отслеживает изменения `dirId`
- ⚠️ Но можно улучшить: добавить проверку, что `currentDirectory` загружен перед передачей в `FilesList`

**Вариант улучшения:**
```vue
<FilesList 
  v-if="selectedAudit && currentDirectory"
  :dir-id="currentDirectory.key" 
/>
```

### 2. ⚠️ Проблема: Очистка файлов при смене ревизии

**Текущая ситуация:**
- При смене ревизии `currentDirectory` может стать `null` на время загрузки
- `FilesList` получит `dirId = null`, что может очистить список файлов
- Это может вызвать "мигание" интерфейса

**Предложение:**
- ✅ Это нормальное поведение - файлы должны очищаться при смене директории
- ⚠️ Но можно улучшить UX: показывать состояние загрузки вместо пустого списка

**Вариант улучшения:**
```vue
<FilesList 
  v-if="selectedAudit"
  :dir-id="currentDirectory?.key ?? null"
  :loading="loadingDirectory"
/>
```

### 3. ✅ Проверка: Корректность API

**Текущая ситуация:**
- `filesApi.getFilesByDirId(dirId)` должен возвращать файлы, где `af_dir = dirId`
- Это соответствует связи один-ко-многим в БД

**Предложение:**
- ✅ Проверить, что API endpoint корректно фильтрует файлы по `af_dir`
- ✅ Убедиться, что backend возвращает правильные данные

### 4. ⚠️ Проблема: Отсутствие обработки ошибок

**Текущая ситуация:**
- Если директория не найдена, `currentDirectory = null`
- `FilesList` получит `dirId = null` и не загрузит файлы
- Но нет явной обработки случая, когда директория не найдена

**Предложение:**
- ✅ Добавить явную обработку случая отсутствия директории
- ✅ Показывать сообщение пользователю, если директория не найдена

**Вариант улучшения:**
```vue
<FilesList 
  v-if="selectedAudit && currentDirectory"
  :dir-id="currentDirectory.key" 
/>
<div v-else-if="selectedAudit && !loadingDirectory && !currentDirectory" class="text-grey-7">
  Директория для этой ревизии не найдена
</div>
```

---

## Рекомендации

### ✅ Что оставить как есть:

1. **Цепь загрузки:** Текущая реализация корректна
2. **Watch в FilesList:** Автоматическая загрузка при изменении `dirId` работает правильно
3. **Передача props:** `:dir-id="currentDirectory?.key ?? null"` корректна

### 🔧 Что можно улучшить:

1. **Условие отображения FilesList:**
   ```vue
   <!-- Текущее: -->
   <FilesList v-if="selectedAudit" :dir-id="currentDirectory?.key ?? null" />
   
   <!-- Предложение: -->
   <FilesList 
     v-if="selectedAudit && currentDirectory" 
     :dir-id="currentDirectory.key" 
   />
   ```
   **Преимущества:**
   - Не передаём `null` в `dir-id`, если директория ещё не загружена
   - Избегаем лишних вызовов API с `null`
   - Более явное условие отображения

2. **Обработка случая отсутствия директории:**
   ```vue
   <FilesList 
     v-if="selectedAudit && currentDirectory" 
     :dir-id="currentDirectory.key" 
   />
   <div v-else-if="selectedAudit && !loadingDirectory && !currentDirectory" class="q-pa-md">
     <q-banner class="bg-blue-1 text-blue-9">
       Директория для этой ревизии не найдена
     </q-banner>
   </div>
   ```

3. **Добавить состояние загрузки:**
   - Можно передать `loadingDirectory` в `FilesList` для синхронизации состояний загрузки
   - Но это не критично, так как `FilesList` имеет своё состояние загрузки

---

## Выводы

### ✅ Текущая реализация корректна:
- Цепь "ревизия → директория → файлы" реализована правильно
- Связь один-ко-многим работает через `dirId` prop
- Автоматическая загрузка файлов при изменении директории работает

### 🔧 Рекомендуемые улучшения:
1. Изменить условие отображения `FilesList` на `v-if="selectedAudit && currentDirectory"`
2. Добавить обработку случая отсутствия директории
3. Убрать `?? null` из `:dir-id`, так как компонент не будет отображаться без директории

---

**Дата создания:** 2026-01-19
