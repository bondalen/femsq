# СУДЗ — эскизы экранов MVP (G4)

**Дата создания:** 2026-08-07  
**Последнее обновление:** 2026-08-15 (S62: блокер — экран Договоры / cnNum)  
**Статус:** экран A0 = yr; Progress — лаунчер **операций**; бывший Rslt → «Долги / мероприятия»; **C — лаунчер загрузки свода**; для `CnExistCtptNotLoad` нужен TopBar **Договоры** ([02-10](./02-10_contracts-cnNum-access.md))  
**Домен:** [01-overview](../domain/sudz/01-overview.md) · процесс [03-processes](../domain/sudz/03-processes.md) · витрина [08-target-schema §3.6](../domain/sudz/08-target-schema.md) · алгоритм Access [04 §2.7](../domain/sudz/04-data-model.md#27-полный-алгоритм-btncidufload_click--цепочка-сопоставления-подтверждено-s29)  
**IA:** [02-4_app-forms-ia.md](./02-4_app-forms-ia.md)  
**План:** [chat-plan-26-0802-sudz.md](../chats/chat-plan/chat-plan-26-0802-sudz.md) §5.6  
**Задачи:** 0065–0070 (СУДЗ); **активно: 0069**  
**Access pmt (1.1.1.2, не эскиз FEMSQ):** [02-11_cn-inv-pmt-upl-access.md](./02-11_cn-inv-pmt-upl-access.md)

---

## 0. Приёмка витрины (вход в UI)

Контракт песочницы **принят для MVP** (S42–S46):

| Proc / VIEW | Назначение |
|-------------|------------|
| `sudz.Yr_DbtChanges(@yr)` | Rslt для специалистов (нарастающие срезы); для FEMSQ — расширение `@as_of_upl` |
| `sudz.Yr_DbtChangesD644(@yr, @curr_upl)` | Итоговый построчный документ |
| `sudz.Yr_DbtChangesD644Svod(@yr, @curr_upl)` | Годовой свод по счетам (форма+формулы; полные суммы — после seed портфеля) |

Схема **`sudz`** — DEV-контур MVP (S48 / 0066). Лаборатория `test_sudz` сохранена.  
Оговорки: сравнение по **значениям**, не по строкам дат в именах seed; шапка письма — отчётный слой; история Access-миграции вне MVP (S24).

---

## 1. Место в TopBar

```text
СУДЗ ▾
  ├── Портфель года                 ← экран A0 (yr) — основной бизнес-контур года
  ├── Долги / мероприятия           ← экран A (бывш. Rslt; сбор mery)
  ├── Исходящие документы           ← экран B (опционально; ядро — Progress на A0)
  └── Загрузка свода                ← экран C (задача 0069; эскиз S61)
```

На MVP без ролей (I1/S27) — все пункты доступны всем.

**Решение владельца S51:** пункт «Портфель года» = форма **`yr`** (не список долгов). Список долгов Rslt переименован в «Долги / мероприятия».

**Решение владельца S52 / S52a:** формирование Rslt / D644 / свода — на вкладке **«Ход (Progress)»** карточки года. Указатель **`yr_CmmGr_New`** — только для Rslt повтор. Отдельный TopBar «Исходящие» на первом срезе 0070 не обязателен.

---

## 1a. Экран A0 — «Портфель года» (`yr`)

**Цель:** вести год-вариант: база выгрузки, группа комментариев, перечень выгрузок года и связи с выгрузками платежей; просмотр хода (`yr_Progress`); **запуск** промежуточных и исходящих документов.

**Данные (префикс схемы = `sudz` на DEV / `ags` на prod — один код):**  
`yr`, `yr_upl_p`, `cn_inv_dbt_upl`, `cn_inv_dbt_upl_g_p`, `cn_inv_pm_upl`, `cnInvCmmGr`; `yyyy` — всегда `ags.yyyy`.

**CRUD:** да (новые годы и выгрузки заводятся здесь). `yr_Progress` — **read-only** (лог; append при формировании — по желанию реализации).

### Wireframe

```text
┌─ TopBar: … │ СУДЗ ▾ │ … ─────────────────────────────────────────────┐
├─ Портфель года                                      [+ Год] [Обновить]┤
│ ┌─ ~15% ──────────┬─ карточка выбранного yr ───────────────────────┐ │
│ │ yr_variant      │ Поля yr (+ lookup):                            │ │
│ │ [2026 осн.] ◄── │  variant | база | yyyy | CmmGr |[Сохр.]            │
│ │ [2025 осн.]     │────────────────────────────────────────────────│ │
│ │ …               │ Вкладки: [ Выгрузки ] [ Ход (Progress) ]       │ │
│ │                 │ Выгрузки (yr_upl_p), expand как агенты cst:    │ │
│ │                 │  ▸ upl_name | upl_date | uplStatusOnDate       │ │
│ │                 │     └ pm: cn_inv_pm_name | cn_inv_pm_date      │ │
│ │                 │ Progress: лаунчер документов + лог + прототип  │ │
│ └─────────────────┴────────────────────────────────────────────────┘ │
```

### Колонки / lookup (S51)

| Место | Поля |
|-------|------|
| Слева | только `yr_variant` |
| Шапка yr | `cn_inv_dbt_upl` → `upl_name`+`upl_date`; `yyyy` → `ags.yyyy`; `yr_CmmGr` → `cnicgName`+`cnicgDate` (`yr_CmmGr_New` — только Progress) |
| `yr_upl_p` | `upl_name` + `upl_date` + `uplStatusOnDate` |
| Раскрытие `g_p` | `cn_inv_pm_name` + дата; lookup и по `cn_inv_dbt_upl`, и по `cn_inv_pm_upl` |

### Вкладка «Ход (Progress)» — лаунчер операций (S52 / S58)

Контекст: выбранный **`yr`**. Выбор и CRUD **`yr_CmmGr_New`** — **только** на Progress при «· Загрузить» (в шапке года поля New нет).

```text
Операция: [ Rslt сбор · Выгрузить | Rslt повтор · Выгрузить | Rslt повтор · Загрузить | D644 · Выгрузить | Свод · Выгрузить ]
Срез:     [ из yr_upl_p ▾ ]              ← для · Выгрузить (Rslt: asOfUpl; D644/Свод: currUpl)
Группа новых: [ 904 · имя · дата ]       ← только «Rslt повтор · Выгрузить» (read-only)
Результат:(•) предпросмотр  ( ) Excel   ← только выгрузка Rslt
Папка:    [ test ] [📁]                  ← Excel (Rslt / D644 / Свод)

── если «Rslt повтор · Загрузить» ──
Группа новых (yr_CmmGr_New): [ 904 ▾ | + Создать ]
Файл возврата:               [ …xlsx ] [📎]

[ Выполнить ]
── подвкладки: Лог (yr_Progress) | Предпросмотр ──
```

**Предпросмотр Rslt:** native HTML-таблица в scroll-рамке (не FemsqTable — wide grid / sticky / cell-click); те же колонки, что в Excel; у сбора `*_new` пустые; **многострочные** заголовки (до 3 строк) + **поколоночные** фильтры (AND с глобальным); ячейки в одну строку, полный текст — в нижней панели; sticky шапка+фильтры при вертикальном скролле; кнопка «Выгрузить в Excel».

| Операция | Срез | Результат/папка | CmmGr_New + файл |
|----------|------|-----------------|------------------|
| **Rslt сбор · Выгрузить** | asOfUpl | да (proto/Excel) | нет (`*_new` пустые) |
| **Rslt повтор · Выгрузить** | asOfUpl | да (proto/Excel) | **read-only** (откуда `*_new`) |
| **Rslt повтор · Загрузить** | — | — | **да** → upsert в `yr_CmmGr_New` |
| **D644 · Выгрузить** | currUpl | да (proto/Excel) | нет (`yr_CmmGr`) |
| **Свод · Выгрузить** | currUpl | да (proto/Excel) | нет (`yr_CmmGr`) |

**S52a:** все выходные, кроме Rslt повтор · Выгрузить, читают только **`yr_CmmGr`**. Загрузка возврата всегда пишет в **`yr_CmmGr_New`** года (не комбо «подгрупп Access»).  
Процесс: [03 §1.1.2–1.1.3](../domain/sudz/03-processes.md).

**Шапка письма** — только для Excel D644/свода.

**Порядок реализации (0070):** (1) Rslt сбор → (2) Rslt повтор (+ DDL New) → (3) **D644/свод** ✅ S59; импорт возврата — Progress · Загрузить (S58).

**Папка Excel:** File System Access API (имя папки; **полный путь — отложен до отдельного решения**, S60). После операции — запись в `yr_Progress` (**новые строки сверху**, хранится до **~100** строк; старые отбрасываются).

**Excel Rslt:** стили эталона 26-0212 (S55); подпись idNum — «№ задолженности в СФ» (S56).
### Таблицы UI

**Списки** (годы, долги, стройки) — **FeQuLib** (`FemsqTable`). **Предпросмотр Rslt** — native grid (FeQuLib пока без sticky/wide-scroll/cell-click/multiline header). Раскрытие выгрузок — паттерн как дерево агентов на экране Строек.

---

## 2. Экран A — «Долги / мероприятия» (бывш. Rslt)

**Цель:** показать долги годового портфеля нарастающим итогом по срезам; дать ввод/правку куратора, мероприятий, кода стройки (**1.1.2.***; до вехи **1.1.3** — в группу `yr_CmmGr_New`, после вехи / для «официальных» — `yr_CmmGr`).

**Данные:** GraphQL → `sudz.Yr_DbtChanges`; запись → `sudz.cnInvCmm*` / целевая группа по этапу (`yr_CmmGr_New` на сборе, `yr_CmmGr` после вехи — уточнить при коде).

### Wireframe

```text
┌─ TopBar: … │ СУДЗ ▾ │ … ─────────────────────────────────────────────┐
├─ Заголовок: Долги / мероприятия · yr 901            [Обновить] [⋯] ─┤
│  Год: [2026 ▾]   База: 31.12.2025 (upl 901)   Актуал. группа: II.26 │
│  Фильтр: [счёт ГК ▾] [поиск контрагент/договор/СФ…        ] [Сброс] │
├─────────────────────────────┬───────────────────────────────────────┤
│ Список долгов (FemsqTable)  │ Карточка долга                        │
│ dbtKey│СГК│Контрагент│Overd│ │ 82 · 606012 · Газстройпром, АО        │
│  basе │док│стройка     │…  │ │──────────────────────────────────────│
│  82   │… │7947│70.5M │◄──│ │ Реквизиты (base / curr по вкладкам)    │
│  85   │… │А19 │9.5k  │   │ │  Договор · дата · ИНН · срок · суммы   │
│  …    │  │   │       │   │ │  СФ по срезам: А19 → 90 (только чтение)│
│                         ▲   │ │──────────────────────────────────────│
│                         │   │ │ Сбор на дату отчёта                  │
│                         │   │ │  Куратор / Мероприятия / Код стройки │
│                         │   │ │  [Сохранить]                         │
│                         │   │ │ Срезы + превью D644                  │
└─────────────────────────┴───────────────────────────────────────┘
```

### Поведение

1. Выбор года → загрузка витрины; master — одна строка на `dbtKey`.
2. Выбор строки → detail: реквизиты base/curr, история СФ по `upl_date`, поля сбора.
3. **Сохранить** пишет type=1/8 и cst в целевую группу этапа (на цикле сбора — **`yr_CmmGr_New`**; после вехи 1.1.3 / вне сбора — **`yr_CmmGr`**). Excel-колонки `*_new` — слой отчёта, не отдельная таблица.
4. Пустой портфель / ошибка GraphQL — empty/error как на экране организаций.
5. Колонки списка: минимум СГК, контрагент, base Overd, документ (base `invNumEnum`), код стройки года, куратор.

### Вне MVP экрана A

- Полный паритет всех колонок Access Rslt в одной таблице (достаточно detail + ключевые колонки списка).
- Согласование разделов (E5).

---

## 3. Экран B — «Исходящие документы»

**Цель (исторический эскиз G4):** превью и выгрузка D644 / свода.

**S52:** ядро перенесено на **A0 → Progress**. Экран B на MVP **не обязателен**; при появлении — тонкая обёртка тех же параметров (`yr`, срез, тип) без дублирования бизнес-логики.

### Wireframe (если оставляем пункт меню)

```text
┌─ TopBar: … │ СУДЗ ▾ │ … ─────────────────────────────────────────────┐
├─ Заголовок: Исходящие документы                     [Обновить]      ─┤
│  Год: [2026 ▾]   → переход/встраивание лаунчера Progress года       │
│  (те же тип / срез / прототип|Excel / Сформировать)                 │
└──────────────────────────────────────────────────────────────────────┘
```

### Поведение

1. Год + срез (`asOfUpl` / `curr_upl`) обязательны по типу документа.
2. Тип «Свод» доступен технически всегда; бизнес-смысл — после Q4 (подсказка в UI).
3. Превью = результат proc / отчётного слоя; выгрузка не меняет БД (кроме опционального append в `yr_Progress`).
4. Документ в D644 = base `invNumEnum` (решение S44).

---

## 4. Связь с процессом P1

| Шаг процесса | Экран |
|--------------|--------|
| 1.1.1.3 выгрузка Rslt (сбор) | A0 Progress · «Rslt сбор» |
| 1.1.2.1 первые результаты | запись в **`yr_CmmGr_New`** (импорт позже; пока UI A) |
| 1.1.2.2 циклы + внесение/контроль | A0 Progress · «Rslt повтор»; правка **`yr_CmmGr_New`** (экран A) |
| 1.1.3 веха «данные внесены» | Решение оператора: правки комментариев прекращены (**S60**); отдельной операции New→`yr_CmmGr` нет |
| 1.2 итоговый / сводный | A0 Progress · D644 / Свод (`yr_CmmGr`) |
| 1.1.1.1 загрузка свода | **экран C** «Загрузка свода» (0069) |

---

## 4a. Экран C — «Загрузка свода» (`cn_inv_dbt_upl` + лаунчер File_f)

**Цель:** операционный контур шага **1.1.1.1** — список пакетов выгрузки ДЗ, лаунчер файла/флагов, лог хода, слот очереди неоднозначностей. Аналог Access `CnInvDbtUpl_2` + вложенная `CnInvDbtUpl>File_f` (**без** apply воронки `btnCidufLoad` в первом UAT экрана).

**Не путать с:** A0 → вкладка **«Выгрузки»** (`yr_upl_p` — привязка уже существующих `cn_inv_dbt_upl` к **году**). Экран C — создание/ведение пакетов и запуск загрузки свода; привязка к `yr` остаётся на A0.

**Скрины Access (эталон):** [assets/26-0811-cn-inv-dbt-upl/](./assets/26-0811-cn-inv-dbt-upl/README.md) — runtime + design (14 файлов, 2026-08-11).

**Данные списка:** `cn_inv_dbt_upl` (`sudz` / `ags`): `upl_key`, `upl_date`, `upl_name`, `uplStatusOnDate`. Access также использует `ags_q_cn_inv_dbt_upl_sum` (design списка/сумм).

**Access File_f (локальные поля):** `cidufPath`, **`cidufFlLoad`** («Обновлять» = писать в БД), **`cidufFlTbl`** («обнов. по исх?» = перечитать Excel → буфер `CnInvDbtUplTbl`), `btnCidufLoad`, `cidufLoadingProgress` (**RTF**/HTML), `cidufKey`/`cidufUpload`; подвкладки листов (`CnInvDbtUplFileSh`) и `InvDouble`.

**Порядок 0069 (S61):** (1) экран-лаунчер без apply → приёмка; (2) воронка по шагам. Полный UNC-путь — отложен (S60 → FSA имя файла). Шаг **1.1.1.2** — вне первого среза.

### Где живут данные (SQL vs Access-local) — после S61c/S61d (DEV)

| Контур UI | Источник в Access | На MS SQL Server (`sudz`) | FEMSQ v1 (этап 6) |
|-----------|-------------------|--------------------------|-------------------|
| Список / CRUD upl | linked `ags_cn_inv_dbt_upl` | **`cn_inv_dbt_upl`** | GraphQL `sudzUplLookups` / `createSudzUpl` |
| Путь, флаги, лог | **`CnInvDbtUplFile`** | **`CnInvDbtUplFile`** | GraphQL `sudzDbtUplLauncher` / `updateSudzDbtUplFile` |
| Перечень листов | **`CnInvDbtUplFileSh`** | **`CnInvDbtUplFileSh`** | read в лаунчере |
| Staging строк Excel | **`CnInvDbtUplTbl`*** | **есть** (пусто до воронки) | этап 7 |
| Очередь неоднозначностей | **`CnInvDbtUplFileInvDouble`** | **есть** (пусто) | каркас грида; ≠ VIEW `ags.cn_inv_dbt_double` |
| Счета / долги (внешн. вкладки) | queries → `cn_inv_dbt` | **`ags.cn_inv_dbt`** | вкладки disabled |
| Выгрузки платежей | `cn_inv_dbt_upl_g_p` + pm | **есть** | вкладка disabled; Pmt staging — prep 1.1.1.2 |

**Экран:** TopBar **СУДЗ → Загрузка свода** → `SudzDbtUplView` (`sudz-dbt-upl`). Кнопка «загрузка» — stub (этап 7).



### Решение по раскладке (после скринов)

Access: **список сверху / детали снизу** (не master слева, как A0). Для экрана C предлагается **тот же вертикальный master–detail**.

### Wireframe v1 (уточнённый)

```text
┌─ TopBar: … │ СУДЗ ▾ → Загрузка свода ────────────────────────────────┐
├─ Загрузка свода                            [+ Выгрузка] [Обновить] ─┤
│ ┌─ список cn_inv_dbt_upl (сверху, ~28%) ───────────────────────────┐ │
│ │ upl_date │ upl_name                         │ upl_key │ срез*   │ │
│ │ 24.01.25 │ Дт Задолженность на 31.12.2024… │ 26      │ 31.12…  │ │
│ │ …        │ …                                │ …       │ …       │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│ Выбрано: 24.01.2025 · Дт Задолженность на 31.12.2024 (Общий свод)   │
│                                                                      │
│ ┌─ главные вкладки (Access-паритет; v1 активна только «загрузка») ─┐ │
│ │ [ загрузка ] [ выгрузки платежей ] [ счета, сумма ]               │ │
│ │              [ задолженности, правка ] [ задолженности, чтение ]  │ │
│ │   (остальные — disabled / «позже»; ярлыки опциональны)            │ │
│ │                                                                    │ │
│ │  Файл: [ D:\…\Дт Задолженность….xlsx ]  (cidufPath, как в Проводнике) │ │
│ │  ☑ Обновлять (cidufFlLoad)   ☑ обнов. по исх? (cidufFlTbl)        │ │
│ │  [ загрузка ]  ← excelToTbl если «обнов. по исх?», плюс префикс     │ │
│ │                                                                    │ │
│ │  Панель шагов воронки (S61f; без дубля Excel→Tbl):                 │ │
│ │  ☑ orgNotInBuirg  ☑ CnNotLoad  ☑ …  ☐ ExistDbl*                    │ │
│ │  * CnCtptInvAccExistDbl — disabled (как в Access с 03.02.2023)     │ │
│ │  Пресеты: org | до договоров dry-run | полная dry-run | + apply    │ │
│ │                                                                    │ │
│ │  Подвкладки File_f:                                                │ │
│ │  [ ход загрузки ] [ перечень листов ] [ повторяющиеся СФ ]         │ │
│ │                                                                    │ │
│ │  · Ход: HTML/лог (cidufLoadingProgress; в Access — RTF→HTML)       │ │
│ │  · Листы: грид лист | счёт | проверять?                            │ │
│ │  · Повторы СФ: каркас InvDouble + место под «создать СФ…»          │ │
│ └────────────────────────────────────────────────────────────────────┘ │
│ * срез = uplStatusOnDate (в Access-гриде нет — колонка на решение)   │
```

### Панель шагов воронки (S61f) — решение владельца

**Мотив:** в Access ранние ошибки воронки портят поздние шаги; оператор/разработчик включал `Sub` по одному (комментарии в [`Form_CnInvDbtUpl_gt_File_f.cls`](../../../project/proposals/vba-analysis/VBA-Code-Export/Form-Modules/Form_CnInvDbtUpl_gt_File_f.cls) ≈ 140–247).

**FEMSQ:** чекбоксы stepId + жёсткий порядок (префикс цепочки) + `cidufFlLoad` (лог vs запись). Реестр stepId и правила — [chat-plan §5.6 этап 7](../chats/chat-plan/chat-plan-26-0802-sudz.md#56-0069-загрузка-общего-свода--порядок-s61--s61c). Инлайн «новые организации» → именованный шаг `orgNotInBuirg`.

**Код (S61g):** панель в `SudzDbtUplView` + GraphQL `runSudzDbtUplFunnel` (остальные шаги stub).

**Код (S61h):** `excelToTbl` реален — Apache POI → `CnInvDbtUplTbl` (при `cidufFlTbl`).

**Код (S61j):** лог хода заменяется новым прогоном; Excel→Tbl только с «обнов. по исх?».

**Код (S61k):** `orgNotInBuirg` — лог организаций без кода БУиРГ (JOIN `ags.org_id`/`ags.og`); домен и `CnNotLoad` не трогаются.

### Поля UI ↔ источники

| Зона | Поле UI | Access / источник | v1 |
|------|---------|-------------------|----|
| Список | `upl_date`, `upl_name`, `upl_key` | datasheet `CnInvDbtUpl_2` | да |
| Список | дата среза | `uplStatusOnDate` | **на решение** |
| Шапка | дата + имя | полоса под списком Access | да |
| Лаунчер | файл | `cidufPath` целиком (Проводник); без скрепки | да |
| Лаунчер | «Обновлять» | **`cidufFlLoad`** — apply вставок в БД | UI-state |
| Лаунчер | «обнов. по исх?» | **`cidufFlTbl`** — Excel → staging | UI-state |
| Лаунчер | «загрузка» | `btnCidufLoad` | stub → этап 7: прогон **отмеченных** шагов (S61f) |
| Лаунчер | панель шагов | комментирование Sub в VBA | **док S61f**; UI/код — позже |
| Ход | лог | `cidufLoadingProgress` (RTF→HTML) | слот |
| Листы | грид | `CnInvDbtUplFileSh` | **каркас** |
| Повторы | грид + кнопка | `InvDouble` | **каркас** |
| Внешние вкладки | pm / счета / долги | `g_p`, sum-query, `cn_inv_dbt` | ярлыки optional; контент вне UAT v1 |

### Поведение v1 (без воронки)

1. TopBar **СУДЗ → Загрузка свода** (сейчас `disable` — включить при коде UI).
2. Список сверху, сорт. `upl_date` DESC; выбор → шапка + вкладка «загрузка».
3. **[+ Выгрузка]** — CRUD upl (имя, даты); файл не обязателен до реального запуска.
4. Файл — редактируемый `cidufPath` (как в Проводнике); Java открывает файл по этому полю (S61i).
5. Оба флага видны и переключаются; на stub не влияют до шага 2.
6. Три подвкладки — каркас; лог пустой / placeholder.
7. Связь upl↔`yr` — только на A0 (`yr_upl_p`).

### Вне первого среза экрана C / 0069

- Apply воронки и запись в Dbt / `cn_inv_dbt*`.
- Живой разбор InvDouble; наполнение листов из Excel.
- Контент вкладок «счета / задолженности / платежи» (скрины сохранены).
- Полный путь файла/папки (S60).
- Шаг **1.1.1.2** (`export_*`).

---

## 5. Критерии приёмки эскизов

- [x] Экраны A0 / A / B описаны (структура, wireframe, поведение).
- [x] Вписаны в TopBar «СУДЗ».
- [x] Опора на принятые proc витрины.
- [x] S52/S52a: Progress-лаунчер; `yr_CmmGr_New` только для Rslt повтор; веха 1.1.3.
- [x] S53: Rslt сбор — прототип + Excel (`asOfUpl`, пустые `*_new`).
- [x] S59: D644 / Свод · Выгрузить (Excel, `currUpl`).
- [x] S59b: подтверждение владельцем среза D644/Свод · Выгрузить (UAT «приемлемо», 2026-08-09); мелкие правки полей — по ходу.
- [x] S59c: Свод proto/Excel; лог Progress (сверху, ≤100, единое время).
- [x] S60: веха 1.1.3 = прекращение правок комментариев (без операции New→CmmGr); **полный путь папки выгрузки** — отложен до отдельного решения (имя папки / FSA как сейчас).
- [x] Задача **0070** закрыта по объёму Progress MVP.
- [x] **S61e / 0069 этап 6:** экран C «Загрузка свода» (GraphQL + UI; «загрузка» stub).
- [x] **S61f / 0069:** метод этапа 7 зафиксирован — панель шагов с чекбоксами (док; код не начат).
- [x] **S61g / 0069:** UI панель + stub-оркестратор.
- [x] **S61h / 0069:** excelToTbl → CnInvDbtUplTbl.
- [x] **S61i / 0069:** путь Excel = поле БД `cidufPath`; скрепка снята.
- [x] **S61j / 0069:** лог очищается; чекбокс excelToTbl снят.
- [x] **S61k / 0069:** orgNotInBuirg — только лог (без записи в домен).
- [x] **S61n / 0069:** CnExistCtptNotLoad — только лог.
- [x] **S62:** эталон Access `cnNum` сохранён; экран «Договоры» вынесен в **0071** (блокер ручного разбора исполнителя).
- [ ] **S61 / 0069:** эскиз экрана C «Загрузка свода» принят владельцем (wireframe + поля; без apply).
- [ ] **0069 этап 7:** реализация воронки по stepId (S61f).
- [ ] **0071:** UI «Договоры» (минимум для разбора `CnExistCtptNotLoad`) — подход согласуется отдельно.
