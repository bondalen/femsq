# MSSQL2012 — пакет 26-0714

Единственный набор для **продуктива** (SQL Server 2012 SP4).

Скрипты идентичны корню `../` (DDL через `COL_LENGTH` + `ALTER TABLE ADD`, без конструкций 2016+).

Порядок: `00` → `01` → `04`. Откат: `05`.

См. также: `../README.md`, задача **0051**, chat-plan §9.3.6.1.
