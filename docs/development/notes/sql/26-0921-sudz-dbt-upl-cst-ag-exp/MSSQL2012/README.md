# MSSQL2012 — копии для продуктивного синтаксиса (2012 SP4)

Те же имена файлов, что в корне пакета. Синтаксис без `CREATE OR ALTER` / `DROP IF EXISTS`
(используется `IF OBJECT_ID … DROP` + `CREATE`).

На DEV допустимо применять скрипты из корня или отсюда — тексты совпадают.
