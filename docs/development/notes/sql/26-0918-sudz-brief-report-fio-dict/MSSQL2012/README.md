# MSSQL2012 — словарь FIO (ags.fioDict)

Синтаксис SQL Server **2012 SP4** (без `CREATE OR ALTER` / `DROP IF EXISTS`).  
Применять на продуктиве и как канон на DEV.

Порядок: `00` → `01` → `02` → `04`. Откат: `05`.
