# MSSQL2012 — `CnInvUplSfDouble` (S68)

Синтаксис SQL Server **2012 SP4**. Тот же смысл, что корневой `30_*.sql`.

На продуктиве применять **только** после approve владельца и отдельного `db-upgrade` (сейчас объект только в `sudz` на dev).

**lastUpdated:** 2026-08-16
