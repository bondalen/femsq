# Приёмка помесячных плановых показателей по структуре затрат (dev)

**Дата:** 2026-06-17  
**lastUpdated:** 2026-06-23  
**Автор:** Александр  
**Контекст:** пакет `spMstrg_2606`, тестовая цепь **5**, `@MounthEndDate = '2022-12-31'`  
**Связанные документы:** `08-testing-strategy.md`, `12-dev-acceptance-protocol.md`, `04-computation-map.md`, `03-design-decisions.md` (Решение 14), [`glossary.md`](../../../../../project/glossary.md)

---

## 1. Назначение

Подтвердить на **dev** (Docker `femsq-mssql`), что стек `_2606` корректно обрабатывает **помесячные плановые показатели** уточнённого плана (`ipgUtPlPnLmMn`) с разбивкой по элементам структуры затрат **stCost** и в целом.

На **продуктиве** разбивка UtPl по stCost — отдельная бизнес-задача (наполнение данными организациями, длительный срок). **Процесс prod-деплоя не меняется:** fixture и скрипты фазы 3 **не входят** в `MSSQL2012/` и на флеш-носитель.

Успешная dev-приёмка по планам подтверждает **логику стека**; отсутствие UtPl@172/187/195 на prod до наполнения данных — **не блокер** релиза SQL-пакета.

---

## 2. Область проверки

| Параметр | Значение |
|----------|----------|
| Цепь | `@ipgCh = 5` |
| Дата накопленного плана | `@MounthEndDate = '2022-12-31'` (`dAll` в mastering) |
| Нагрузка | `@stIpg = NULL` (680 контрактов, максимум) |
| Стройки в выборке | только с **помесячной разбивкой** UtPl (`ipgUtPlPnLmMn`) |
| stCost | **212** (итог), **195** (СМР/work), **172** (оборудование/equip), **187** (прочие/others) |

Стройки **без** `ipgUtPlPnLmMn` в отчёт попадают как `SKIP` (не FAIL).

---

## 3. Главный критерий качества плана (К-12)

Для каждой стройки с помесячной разбивкой **накопленный план на дату** должен **строго совпадать с лимитом** `ipgPn` по **каждому** stCost отдельно.

| stCost | Смысл | Эталон лимита | Поле стека (`fnMasteringCstAgPnSh_2606`) |
|--------|-------|---------------|----------------------------------------|
| 212 | Итог | `ipgpSmTtl × 1 000 000` | `agSmmTtl` @212 |
| 195 | СМР (work) | `ipgpSmWrk × 1 000 000` | `agSmmTtl` @195 |
| 172 | Оборудование | `ipgpSmEqu × 1 000 000` | `agSmmTtl` @172 |
| 187 | Прочие | `ipgpSmOth × 1 000 000` | `agSmmTtl` @187 |

Эквивалентно: `ipgPnLim.ipgplLim × 1 000 000` при наличии строки на соответствующем stCost (на цепи 5 согласовано с `ipgpSm*`).

**Пример:** лимит @195 = 100 ₽ → сумма всех месяцев `iuplpmLim@195` = 100 ₽ и `agSmmTtl@195` на `2022-12-31` = 100 ₽.

В PercentBrn накопленный план отображается как `ag_Pl` / `ag_PlAccum` (источник — тот же контур `smm` / `smmTtl`).

---

## 4. Критерий согласованности частей (К-13)

На той же дате и той же стройке (с UtPlMn):

```
agSmmTtl@212  ≈  agSmmTtl@172 + agSmmTtl@187 + agSmmTtl@195
```

Допуск: `ε = 0,01` (money).

К-13 **не заменяет** К-12: сначала каждая статья сходится с лимитом, затем итог сходится с суммой частей.

---

## 5. Двухуровневая проверка (скрипт `07m_plan_limit_conformance_chain5.sql`)

### Фаза 0 — данные `ipgUtPlPnLmMn` (секунды)

По каждому `ipgPn` с UtPlMn и каждому stCost ∈ {212, 195, 172, 187}:

```
sum(iuplpmLim по всем месяцам)  ≈  ipgplLim@stCost
```

При FAIL фаза 1 не запускается.

### Фаза 1 — стек `_2606` (4 прохода TVF)

По каждому `ipgPn` с UtPlMn, **последний** `dAll ≤ @MounthEndDate`, где план ненулевой:

```
COALESCE(agSmmTtl, inSmmTtl, drSmmTtl)  ≈  cum(UtPlMn) × 1 000 000
```

где `cum(UtPlMn)` — накопленная сумма `iuplpmLim` по `iuplpmMn` (любой префикс месяцев; на последней дате освоения стека).

На `2022-12-31` у большинства пн план в стеке **нулевой** (нет строки `dAll`); это не FAIL — берётся последняя дата с планом. `SKIP` — нет ни одной даты с планом по stCost.

**PERF (К-12):** wall-clock **≤ 480 с** на полную цепь (4 stCost × NULL). Факт **~300 с** (2026-06-17).

---

## 6. Аддитивность плана (скрипт `07m_plan_additive_chain5.sql`)

По образцу `07i_COMPARE_stCost_additive_chain5.sql`, поле `COALESCE(ag/in/dr SmmTtl)`, последний `dAll ≤ @dt`, уровень **ipgPn**:

```
plan@212  ≈  plan@172 + plan@187 + plan@195
```

Допуск: `ε = 5,00` (money) — накопление ×1e6 после split fixture по весам `ipgPnLim` (макс. наблюдённый |diff| ≈ 2,06).

**PERF (К-13):** wall-clock **≤ 480 с** на полную цепь. Факт **~302 с** (2026-06-17).

---

## 7. Dev-only fixture (предусловие К-12 / К-13)

Текущие данные цепи 5: `ipgUtPlPnLmMn` только на **stCost 212** (~9708 строк, 1164 пн). Без fixture фазы 0/1 по 195/172/187 **не пройдут**.

Папка (не в `MSSQL2012/`):

`docs/development/notes/sql/26-0604/fixture/dev-chain5-utpl-stcost/`

| Шаг | Файл | Назначение |
|-----|------|------------|
| 1 | `FIXTURE_01_normalize_utplmn.sql` | 6 пн: `sum(UtPlMn@212) → ipgpSmTtl` |
| 2 | `FIXTURE_03_split_stcost.sql` | split каждого месяца @212 → @172/@187/@195 по весам `ipgPnLim`; строку @212 сохранить |
| 3 | `FIXTURE_04_verify_data.sql` | помесячно: `month@212 = month@172+187+195`; суммы = лимиты |
| 4 | `FIXTURE_99_rollback.sql` | откат по журналу вставок |

Опциональная синтетика для 725 пн без UtPlMn — **вне** К-12 (нет помесячной разбивки).

---

## 8. Протокол dev-приёмки (фаза 3)

Выполняется **только на dev**, после fixture, **перед** финальной сборкой флеша (этап 17.2.3+), флаг оркестратора `--with-plan-stcost`.

| # | Шаг | Скрипт | PERF |
|---|-----|--------|------|
| 3a | Fixture apply | `fixture/.../FIXTURE_01,03` | вне бюджета |
| 3b | Верификация данных | `FIXTURE_04_verify_data.sql` | секунды |
| 3c | **К-12** план = лимит | `07m_plan_limit_conformance_chain5.sql` | ≤ 8 мин |
| 3d | **К-13** 212 = 172+187+195 | `07m_plan_additive_chain5.sql` | ≤ 8 мин |
| 3e | Контроль лимитов mastering | `07i` @ `stIpg=NULL` | ≤ 8 мин |

Фазы 1–2 (`run_acceptance_dev_chain5.sh` без флага) — без изменений.

**Суммарный perf-блок фазы 3:** ориентир **≤ ~16 мин** (К-12 + К-13; при необходимости К-12 и К-13 объединяют в один прогон с общим лимитом 960 с).

---

## 9. Формат PASS в логе

```
К-12 | chain=5 | dt=2022-12-31 | ipgPn=N | STACK fail 212/195/172/187=... | SKIP=... | plan@dt(212)=... | ms=... | PASS/FAIL

К-13 | chain=5 | dt=2022-12-31 | slices=N | FAIL_plan_additive=n | ms=... | PASS/FAIL
```

---

## 10. Состояние реализации

| Артефакт | Статус |
|----------|--------|
| Документация (этот файл) | ✅ 2026-06-17 |
| `fixture/dev-chain5-utpl-stcost/` | ✅ 2026-06-17, FIXTURE_04 PASS |
| `07m_plan_limit_conformance_chain5.sql` | ✅ 2026-06-17, К-12 PASS @NULL ~299s |
| `07m_plan_additive_chain5.sql` | ✅ 2026-06-17, К-13 PASS @NULL ~302s, ε=5 |
| `run_acceptance_dev_chain5.sh --with-plan-stcost` | ✅ wired (18.4) |
| Прогон К-12/К-13 PASS | ✅ 2026-06-17, `run_plan_stcost_chain5.sh` |
| `07n_plan_strict_cst_chain5.sql` | ✅ пилот cst 2102, 2026-06-17 |
| `FIXTURE_05_pilot_cst_2102.sql` | ✅ dev-only |

**Готовность к исполнению:** К-12/К-13 (07m) PASS; строгая логика (07n) — **пилот cst 2102 PASS**; FIXTURE_06, 17 дат, полная цепь (18.7.2–18.7.4) — в работе; интерполяция плана/факта — отложено (18.9).

---

## 11. Строгая приёмка (07n, этап 18.7)

Скрипт `07n_plan_strict_cst_chain5.sql`, пилот `FIXTURE_05_pilot_cst_2102.sql` (dev-only).

| Код | Условие |
|-----|---------|
| **К-12b** | У ipgPn, **активной** на `@dt` в цепи: `smmTtl@stCost(@dt)` = лимит |
| **К-12c** | На каждой `dd` fnStCostRsIpgPn: `smmTtl` = cum(UtPlMn через `mKey`) × 1e6 |
| **К-12r** | На `ipgcrvEnd` (окончание актуальности ИП): план = cum(UtPl) на эту дату (смена ИП в цепи) |
| **К-13b** | На каждой `dd`: `plan@212 ≈ @172+@187+@195` |

**Ограничение prod-данных:** все 1164 ipgPn с UtPl на цепи 5 имеют `ipgcrvEnd < 2022-12-31`; UtPl привязан к **снятым** ИП 6/8. Пилот добавляет UtPl на актуальную ipgp **5271** (cst 2102).

---

## 12. Замеры производительности и «наиболее полный тест»

### Ограничение прежних замеров К-6 / К-7

Замеры `07h`, `07h6`, `07f` и аналогичные прогоны **не включали** полноценное определение **плановых показателей** UtPl по всем пунктам ИП и всем контрольным датам цепи. В типичном prod-подобном наборе на 31.12 у большинства пунктов накопленный план в стеке **нулевой** (нет UtPl на активной ИП 11).

**Следствие:** оценки времени работы процедуры в целом (`spMstrg_2606`, `fn2`, полная цепь) **занижены** относительно сценария, когда организации заполняют помесячный UtPl.

### Наиболее полный тест (этап 18.7.4)

Единственный прогон, дающий репрезентативную картину **wall-clock** и соответствия требованиям **К-7** / интерактивности с учётом планов:

- цепь **5**, все **три** ИП (6, 8, 11) с тестовым UtPl в полном объёме (~1889 `ipgp`, ~90k строк `ipgUtPlPnLmMn`);
- матрица **17 дат** (`fnIpgChDatsV`);
- проверки **К-12…К-17** + строгая логика **07n** / **07o** на полной цепи.

Ориентир времени: **30–90 мин** (зависит от dev-окружения); бюджет фиксируется отдельно при внедрении `run_plan_full_chain5.sh`.

До выполнения 18.7.4 заявления о соответствии полной процедуры требованиям по времени считаются **предварительными**.

---

## 13. FIXTURE_06 — изолированные тестовые группы UtPl

Чтобы не смешивать данные приёмки с существующими `ipgUtPlGr` 3, 4, 6:

| Шаг | Содержание |
|-----|------------|
| **FIXTURE_06_00** | Новые группы планов (напр. `ipgUtPlGr` 18–20, `TEST_CH5_IPG6/8/11`); журнал исходных `ipgcrvUtPlGr` |
| **FIXTURE_06_01** | Dev-only: временная подмена `ipgChRlV.ipgcrvUtPlGr` для ИП 6/8/11 цепи 5 |
| **FIXTURE_06_golden** | Золотая стройка **cstAgPn=2102**: полный UtPl для пунктов **2037, 3290, 5271** только в тестовых группах |
| **FIXTURE_06_99** | Откат подмены групп и тестовых планов |

Существующие строки UtPl в группах 3/4/6 **не изменяются**.

---

## 14. Полная цепь 5 (этап 18.7.4)

**Целесообразность:** да, как **финальный регрессионный gate** и источник достоверных perf-метрик с планами.

| Этап | Объём | Блокер релиза SQL |
|------|-------|-------------------|
| 18.7.2a — золотая 2102 + 17 дат | FIXTURE_06, 07n/07o | рекомендуется |
| 18.7.2c — 5–10 пилотных строек | выборка | нет |
| **18.7.4** — полная цепь, все ИП | FIXTURE_07, ~90k UtPlMn | **нет** (но обязателен для perf-заявлений) |

Дополнительные критерии (07o):

| Код | Условие |
|-----|---------|
| **К-14** | На каждой из 17 дат P2: план только **активной** ИП |
| **К-15** | P1 на 31.12: cum = лимит по stCost |
| **К-16** | P2 на 31.12: cum = лимит активного пункта ИП |
| **К-17** | На `ipgcrvEnd`: cum = cum(UtPl) на дату окончания ИП |
| **К-12t** | На датах смены ИП: политика «ужесточения» (Решение 14) |

---

## 15. Промежуточные дни месяца (смена ИП)

**Принято (Решение 14):** сохраняется «ужесточение» из `_2605` — на `ipgcrvEnd` / в месяце перехода в накопленный план входит **полный** месячный прирост UtPl.

**Отложено:** интерполяция плана по дням и согласованное определение факта «на промежуточный день» по датам документов — **отдельная объёмная задача** (этап 18.9 чат-плана). Не блокирует текущую приёмку 18.7 при критериях К-12t.

---

*Автор: Александр*
