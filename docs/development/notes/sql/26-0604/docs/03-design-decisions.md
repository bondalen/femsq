# Архитектурные решения spMstrg_2606

**Дата:** 2026-06-04  
**Задача:** `spMstrg_2606` — DAG-фильтрация и корректная цепь ИПГ  
**Контекст:** решения приняты в ходе анализа этапа 0; фиксируются здесь для обоснования кода

---

## Решение 1: Источник @stNet для DAG-обхода — `ipgCh.ipgcStNetIpg`

**Проблема:** В плане изначально предполагалось использовать `ipg.ipgStRlSh` как источник схемы для `fnStDownAll`. Оба поля дают значение 2 для цепи 15, но семантически разные.

**Принято:** использовать `ipgCh.ipgcStNetIpg`.

**Обоснование:**
- Это атрибут **цепи**, а не конкретной ИПГ — точнее отражает «схема для данной цепи»
- Все существующие функции (`fnMasteringStIpgStCost`, `fnStCostRsStIpg`) берут `@stNet` именно из `ipgCh.ipgcStNetIpg`
- В `fnMasteringStIpgStCost` автор сам оставил комментарий: *«здесь чудно, не может быть структура одна и навсегда!!! Нужно как-то выводить структуру из цепи, видимо по поздней инвестпрограмме»* — и решил через `ipgCh.ipgcStNetIpg`

**Реализация:**
```sql
-- В fnIpgChRsltCstUtl2_2606: JOIN на ipgCh один раз при входе
CROSS JOIN ags.ipgCh ch WHERE ch.ipgcKey = @ipgChKey
-- Затем в фильтре:
AND sp.ipgspSt IN (
    SELECT @ipgStKey UNION SELECT f.strChild
    FROM ags.fnStDownAll(ch.ipgcStNetIpg, @ipgStKey) f
)
```

---

## Решение 2: fnStDownAll + UNION, не fnSt напрямую

**Проблема:** `fnSt(@stNet, @root)` включает корень, `fnStDownAll` — нет. Для `@ipgStKey=21` (листовой узел) `fnStDownAll(2,21)` возвращает 0 строк.

**Принято:** использовать `fnStDownAll(@stNet, @ipgStKey)` + явный `UNION SELECT @ipgStKey` для корня.

**Обоснование:**
- Это устоявшийся паттерн в кодовой базе (подтверждён в `fnMasteringStIpgStCost` и `fnStCostRsStIpg`)
- `fnSt` как инструмент существует, но в таких сценариях не используется
- Семантически чище: «всё поддерево от @ipgStKey включительно»

---

## Решение 3: Не использовать fnIpgStNum и ipgSt-семейство

**Проблема:** В предыдущих расчётах использовалась `fnIpgStNumCstapGr` / `fnIpgStNumCstap` / `fnIpgStNum`, работающие с устаревшим семейством таблиц `ipgSt` (созданы 2021-05-31, изолированы от `ipgPn`).

**Принято:** не использовать `fnIpgStNum` и производные в `_2606`.

**Обоснование:**
- Семейство `ipgSt/ipgStRl/ipgStRlSh/ipgStRlShP` не имеет FK-связей с `ipgPn`, `ipg`, `ipgCh`, `ipgStPn`
- Исторически это был первый вариант (2021) структуры ИПГ, заменённый семейством `st` в 2022
- Активные функции в кодовой базе (`fnMasteringStIpgStCost`, `spIpgCh_StPn_Rslt` и др.) используют только `st`-семейство

---

## Решение 4: ipgChRl_2606 — новая таблица для сроков актуальности

**Проблема (Дефект Б):** `ipg.ipgStr/ipgEnd` — глобальный атрибут ИПГ, а не атрибут связи «цепь → ИПГ». Это приводит к тому, что в месяцах 7–12 одновременно присутствуют ipg=25 и ipg=26.

**Принято:** таблица `ags.ipgChRl_2606` хранит **только** вводимые атрибуты связи; конец периода **не сохраняется** и **не обновляется** вручную — вычисляется автоматически.

| Колонка | Тип | Смысл |
|---------|-----|-------|
| `ipgcrvKey` | int IDENTITY PK | Суррогатный ключ |
| `ipgcrvChain` | int NOT NULL FK→ipgCh | Цепь ИПГ |
| `ipgcrvIpg` | int NOT NULL FK→ipg | Инвестпрограмма |
| `ipgcrvStr` | date NOT NULL | Дата начала актуальности (**единственная дата, вводимая вручную**) |
| `ipgcrvUtPlGr` | int NULL FK→ipgUtPlGr | Группа планов |
| `ipgcrvEnd` | date NULL **computed** | `ags.fnIpgChRlEnd_2606(chain, str)` → `MIN(next.str) − 1 день`; NULL = текущая ИПГ |

**Реализация `ipgcrvEnd` — скалярная функция `ags.fnIpgChRlEnd_2606`:**

Вычисляемый столбец не может напрямую содержать `LEAD` или подзапрос по другим строкам той же таблицы. Обходное решение — скалярный UDF, который вызывается из определения вычисляемого столбца:

```sql
CREATE FUNCTION ags.fnIpgChRlEnd_2606(@chain int, @str date)
RETURNS date AS
BEGIN
    RETURN DATEADD(day, -1, (
        SELECT MIN(t.ipgcrvStr)
        FROM ags.ipgChRl_2606 t
        WHERE t.ipgcrvChain = @chain AND t.ipgcrvStr > @str
    ))
END

ALTER TABLE ags.ipgChRl_2606
    ADD ipgcrvEnd AS (ags.fnIpgChRlEnd_2606(ipgcrvChain, ipgcrvStr));
```

**Преимущества перед представлением:** `ipgcrvEnd` доступен непосредственно при обращении к `ags.ipgChRl_2606` — все функции `_2606` читают базовую таблицу, не требуя отдельного объекта-обёртки. Производительность не является проблемой для справочной таблицы малого размера.

**Запрещено:** `INSERT`/`UPDATE` значения `ipgcrvEnd` — столбец вычисляемый, запись в него невозможна.

**Данные для цепи 15 (первый этап):**

| ipgcrvChain | ipgcrvIpg | ipgcrvStr | ipgcrvEnd (вычисл.) | ipgcrvUtPlGr |
|-------------|-----------|-----------|----------------------|--------------|
| 15 | 25 | 2024-11-28 | 2025-07-15 | 17 |
| 15 | 26 | 2025-07-16 | NULL | NULL |

**Почему `ipgcrvUtPlGr = NULL` для ipg=26:**  
В `ags.ipgUtPlGr` нет записи для ipg=26 (последняя — key=17 для ipg=25). «Письмо Д644» не имеет выделенной группы планов на момент реализации.

---

## Решение 5: Параметр @ipgStKey int, не @ipgSt nvarchar

**Принято:** `@ipgStKey int` (числовой ключ `stIpg.stiKey`).

**Обоснование:** `@ipgSt nvarchar` в `_2605` содержит коды типов затрат (`importIpgSt_26-0320.cst_type`), а не структуру ИПГ. Это разные измерения; `_2606` решает другую задачу.

---

## Решение 6: Не исправлять ipg.ipgStr/ipgEnd в продуктиве

**Принято:** `spMstrg_2605` остаётся с известным ограничением по цепи 15 (дефект Б).

**Обоснование:** `_2606` создаётся параллельно как исправленная версия. Изменение `ipg.ipgStr/ipgEnd` затронуло бы все существующие функции, завязанные на эти поля, — слишком широкий сайд-эффект.

---

## Решение 7 (пересмотрено): базовый стек — `fnMasteringStIpgStCost`, не `fnIpgChRsltCstUtl_2408`

**История решения:**

_Первоначально (Q3 = нет @stCostKey)_ предполагалось оставить `fnIpgChRsltCstUtl_2408` как базу и добавить только `@ipgStKey`.

_Пересмотрено_ в ходе анализа (2026-06-04) после исследования `fnMasteringStIpgStCost`, `fnStCostRsCstAgPn`, `fnStCostRsIpgPn`, `ipgPnLim`, `ipgUtPlPnLmMn`.

**Принято:** `spMstrg_2606` строится на стеке `fnMasteringStIpgStCost`, оба параметра `@ipgStKey` и `@stCostKey` включены с nullable-семантикой (NULL = без фильтра).

**Обоснование:**

1. `spMstrg_2605` является **частным случаем** `fnMasteringStIpgStCost` при `@ipgStKey=NULL, @stCostKey=NULL` — это «взять всё, не фильтровать». Разделять на два прохода значит дважды писать по сути один и тот же код.

2. `fnMasteringStIpgStCost` уже содержит **встроенный Дефект А** (комментарий автора: *«здесь чудно, не может быть структура одна и навсегда!!!»*) — там же `max(c.ipgcStNetIpg)`, т.е. всегда берётся последняя ИПГ. Исправление этого требует версионированной копии функций в любом случае.

3. Для цепи 15 `ipgPnLim` и `ipgUtPlPnLmMn` пусты, однако данные планируется наполнить. Для сохранения совместимости в `fnStCostRsIpgPn_2606` вводится **расширенный fallback** к `ipgUtPlP.iuplpLim` (суммарный лимит), что позволяет функции работать ДО наполнения `ipgPnLim`.

**Реализация: новые DB-объекты (9 шт.)**

| Объект | Тип | Назначение |
|--------|-----|-----------|
| `ags.ipgChRl_2606` | TABLE | Хранит `ipgcrvStr` (начало в цепи) |
| `ags.vIpgChRl_2606` | VIEW | + вычисляемый `ipgcrvEnd` |
| `ags.fnIpgChDats_2606` | IF | Генератор дат цепи с точкой разрыва |
| `ags.fnStCostRsIpgPn_2606` | TF | Фикс Деф.Б + fallback к `ipgUtPlP.iuplpLim` |
| `ags.fnStCostRsCstAgPn_2606` | TF | Использует `_2606`-подфункцию |
| `ags.fnMasteringCstAgPnSh_2606` | TF | Использует `_2606`-подфункции |
| `ags.fnMasteringStIpgStCost_2606` | IF | Фикс Деф.А (stNet per-IPG из `ipgChRl_2606`), `@ipgStKey`/`@stCostKey` nullable |
| `ags.fnIpgChRsltCstUtl2_2606` | TF | Обёртка: добавляет метаданные, совместима по формату с `_2605` |
| `ags.fnIpgChRsltCstUtlPercentBrn_2606` | IF | Процентный показатель |
| `ags.spMstrg_2606` | P | Хранимая процедура |

---

## Целевая сигнатура `spMstrg_2606`

```sql
EXEC ags.spMstrg_2606
    @ipgCh          int,           -- цепь ИПГ
    @MounthEndDate  date,          -- последний день расчётного месяца
    @ipgStKey       int    = NULL, -- узел stIpg (NULL = без фильтра по разделу ИПГ)
    @stCostKey      int    = NULL, -- узел stCost (NULL = без фильтра по статье затрат)
    @saveToTables   bit    = 0     -- 0: SELECT×7, 1: INSERT в spMstrg_2606_ResultSet1..7
```

**Инварианты:**
- `spMstrg_2606(15, '2025-07-31', NULL, NULL)` ≡ `spMstrg_2605(15, '2025-07-31', NULL)` (с поправкой на исправленные дефекты)
- `@stCostKey` работает для всех цепей немедленно; для цепи 15 фильтрация станет точной после наполнения `ipgPnLim`
- Параметры независимы: можно фильтровать по одному, по обоим или не фильтровать совсем

### Семантика `@ipgStKey` (фильтр строек, не декомпозиции)

**Принято (2026-06-26):** `@ipgStKey` задаёт **подмножество строек** (`cstAgPn`) в цепи, а **не** срез пунктов ИП или помесячной разбивки UtPl внутри стройки.

| Уровень | Что фильтрует `@ipgStKey` | Что **не** фильтрует |
|---------|---------------------------|----------------------|
| Отбор строек | Стройка попадает в расчёт, если **хотя бы один** её `ipgpPn` связан с узлом `stIpg` (или потомком через `fnStDownAll`) в `ipgStPn` | — |
| Внутри стройки | — | Все пункты ИП цепи (`ipgChRl_2606` × `ipgPn`), декомпозиция `ipgUtPlPnLmMn` по месяцам и stCost, смены ИП 6/8/11 |

После отбора `fnMasteringStIpgStCost_2606` вызывает `fnMasteringCstAgPnSh_2606` **на всю стройку** (`@cstAgPn`), со всеми пунктами ИП и UtPl в тестовых группах.

**Следствия:**

1. Если стройка прошла фильтр при `@ipgStKey = A` и при `@ipgStKey = B`, набор строк по ней в `fn2` / `spMstrg_2606` **идентичен** (проверено dev: `cstAgPn = 2102`, `stIpg` 42 vs 61, `EXCEPT` = 0).
2. Разные `@ipgStKey` меняют только **состав строек** в полном прогоне, не «полноту» одной стройки.
3. **Не путать:** узлы `stIpg` (дерево разделов ИПГ для фильтра отчёта) и **инвестпрограммы** в цепи (`ipgChRl_2606`, ИП 6/8/11 …) — разные измерения. Количество привязок `ipgStPn` у стройки к разным `stIpg` **не означает** «частичный» UtPl в отчёте.
4. **Рекордсет (RS1…7):** до реализации **Решения 16** (этап 19) при `@ipgStKey ≠ NULL` в RS попадают **все** стройки цепи; mastering фильтрует только план/лимит. После Решения 16 RS сужается до IN_GROUP ∪ OUT_GROUP (`stIpgOutLimPn_2606`).

**Ручной прогон одной стройки:** `@ipgStKey`, при котором стройка входит в фильтр (напр. 42 или 61 для golden `cstAgPn = 2102`), либо `@ipgStKey = NULL` + `WHERE cstAgPnKey = …` в ResultSet. См. `13-plan-stcost-monthly-acceptance.md` §17, [`glossary.md`](../../../../../project/glossary.md).

*(параметр `@stNet` не нужен: берётся из `ipgCh.ipgcStNetIpg` → `ipgChRl_2606` per-date внутри функций)*

---

## Решение 8: Параллельное сосуществование — отдельные таблицы ResultSet и независимые клиенты

**Проблема:** В черновике архитектуры `spMstrg_2606` при `@saveToTables = 1` фигурировали те же таблицы `ags.spMstrg_2408_ResultSet1..7`, что используют `spMstrg_2408`, `spMstrg_2605` и FEMSQ/JasperReports. Запись `_2606` в них **перезаписала бы** данные действующих клиентов и нарушила бы принцип «не трогаем `_2605`».

**Принято:** полная изоляция persistent-артефактов по суффиксу версии.  
**Подтверждено пользователем:** 2026-06-04 (согласовано перед Этапом 1).

| Слой | `_2408` / `_2605` (без изменений) | `_2606` (новое) |
|------|-----------------------------------|-----------------|
| SQL Server, режим FEMSQ | `ags.spMstrg_2408_ResultSet1..7` | **`ags.spMstrg_2606_ResultSet1..7`** (CREATE в пакете) |
| Процедура `@saveToTables=1` | `spMstrg_2605` → TRUNCATE/INSERT в `*_2408_ResultSet*` | `spMstrg_2606` → только в `*_2606_ResultSet*` |
| MS Access, локальные таблицы | `ags_fnIpgChRsltCstUtlPercentBrn_2408_tbl_part_*`, `*Rep01_2408_tbl`, … | **Отдельные имена с `_2606`** (создать в Access при переключении; инструкция в deploy-документе) |
| FEMSQ / JasperReports | `execute_spMstrg_2605.sh` + JRXML на `*_2408_ResultSet*` | **`execute_spMstrg_2606.sh`** + смена источника на `*_2606_ResultSet*` (после SQL-приёмки) |

**Структура таблиц:** `spMstrg_2606_ResultSet1..7` — **та же схема столбцов**, что у `spMstrg_2408_ResultSet1..7` (процедура `_2606` строится от того же шаблона SELECT, меняются только имена целевых таблиц и вызовы `fn*_2606`).

**Инварианты совместимости:**
- `spMstrg_2605` и `spMstrg_2408*` продолжают работать как до деплоя `_2606`.
- Деплой `_2606` на продуктив **не обязан** сразу переключать Access/FEMSQ — достаточно создать объекты и таблицы; переключение клиентов — отдельный шаг в `db-upgrade-spMstrg-2606.md`.
- Откат `08_ROLLBACK.sql`: DROP `_2606`-объектов **и** DROP `spMstrg_2606_ResultSet1..7`; таблицы `_2408_ResultSet*` не затрагиваются.

**В пакете SQL (добавить):**
- `05b_CREATE_TABLE_spMstrg_2606_ResultSets.sql` — DDL семи таблиц (dev + `MSSQL2012/`).
- `06_CREATE_PROCEDURE_spMstrg_2606.sql` — только `spMstrg_2606_ResultSet*`.
- `07_VERIFY_after.sql` — COUNT из `spMstrg_2606_ResultSet*`, плюс проверка что `spMstrg_2605` по-прежнему пишет в `*_2408_ResultSet*`.

---

## Решение 9: Двухшаговая модель + суперкласс `factDoc` для всех документов факта (шаг 1)

**Принято:** 2026-06-05 (подтверждено пользователем).

### Двухшаговая модель работ

| | Шаг 1 (этот чат) | Шаг 2 (после приёмки `_2606`) |
|---|---|---|
| **Цель** | Работоспособная `spMstrg_2606` + SQL-пакет для продуктива | Комфортная эксплуатация: клиенты, данные, опциональные улучшения |
| **SQL** | Все объекты `_2606`, `factDoc`/`factDocCost`, триггеры, бэкфилл | Переключение Access/FEMSQ на `*_2606_ResultSet*` |
| **Клиенты** | **Не меняем** VBA, Java, Access | Адаптация по необходимости |
| **Данные** | Ручные правки dev (лимиты цепи 15 и т.п.) — вне пакета или пометка `manual` | Наполнение `ipgPnLim` на продуктиве и др. |
| **Риск** | Минимальный: старый контур изолирован (Решение 8) | Отдельный план работ |

**Инвариант шага 1:** `_2605` / `_2408`, `fnStCostRa`, `ra_summCt` и текущие клиенты работают как до деплоя. Новый контур `_2606` — параллельный, не заменяющий.

### Суперкласс `factDoc` — все типы документов в шаге 1

**Принцип:** на шаге 1 **все** типы документов факта, участвующие в расчёте `_2606`, переводятся на единую схему `factDoc` + `factDocCost`. Частичная реализация (только RA) **не допускается** — теряется однообразие стека и смысл суперкласса.

**Минимальная схема (без изменений от ранее согласованного):**

| Таблица | Назначение |
|---------|------------|
| `ags.factDoc` | `fdKey` PK; `fdDocType` nvarchar; `fdNKey` int — обратная связь с подклассом |
| `ags.factDocCost` | `fdcoFd` → factDoc; `fdcoStCost` → stCost; `fdcoSumm` money *(префикс `fdco` = fact **D**oc **Co**st)* |

**Подклассы и привязки (шаг 1):**

| `fdDocType` | Таблица-подкласс | Колонка FK | Плоские поля → `fdcoStCost` |
|-------------|------------------|------------|------------------------------|
| `RaSumm` | `ra_summ` | `ras_fdKey` | total→212, **work→195**, equip→172, others→187 |
| `RaChangeSumm` | `ra_change_summ` | `racs_fdKey` | total→212, **work→195**, equip→172, others→187 |
| `OgAgFeeP` | `ogAgFeeP` | `oafp_fdKey` | `oafpTotal`→148 |
| `RalpRaAu` | `ralpRaAu` | `ralpra_fdKey` | `ralpraCostAndVat`→150 |
| `PrDocP` | `cn_PrDocP` | `pdp_fdKey` | `costVAT`→205 (ZPTG/ZKTG) или →197 (ZUGH) по `pdtoCode` |
| `CstAgPnMnrl` | `cstAgPnMnrl` | `am_fdKey` | `amSum`→169 |

**Триггеры `AFTER INSERT, UPDATE`:**

- На INSERT: создать строку в `factDoc`, присвоить `*_fdKey` подклассу.
- На INSERT/UPDATE с суммами: синхронизировать `factDocCost`; для VBA — фильтр `UPDATE(column)` (срабатывает только когда плоское поле реально записано).
- В части синхронизации `factDocCost` читать `*_fdKey` из **реальной таблицы** (JOIN), не из `inserted` — иначе одношаговый INSERT Java не получит `fdKey` в той же операции.

**Функции `_2606` читают только `factDocCost`:**

| Старая функция | Новая `_2606` | Источник сумм |
|----------------|---------------|---------------|
| `fnStCostRa` | `fnStCostRa_2606` | `factDocCost` по `ras_fdKey` |
| `fnStCostRaCh` | `fnStCostRaCh_2606` | `factDocCost` по `racs_fdKey` |
| `fnStCostAgFee` | `fnStCostAgFee_2606` | `factDocCost` по `oafp_fdKey` |
| `fnStCostRalp` | `fnStCostRalp_2606` | `factDocCost` по `ralpra_fdKey` |
| `fnStCostPrDoc` | `fnStCostPrDoc_2606` | `factDocCost` по `pdp_fdKey` |
| `fnStCostMnrl` | `fnStCostMnrl_2606` | `factDocCost` по `am_fdKey` |

Старые `fnStCost*` **не изменяются** — используются `_2605` и прочим legacy-кодом.

**Бэкфилл (скрипт `01d_BACKFILL_factDoc.sql`):**

1. Создать `factDoc` для всех существующих строк подклассов.
2. Заполнить `factDocCost` из плоских полей.
3. Для RA: дополнительно учесть исторические строки `ra_summCt` / `ra_change_summCt` (если есть и не дублируют плоские поля).

**Что остаётся на шаге 2:**

- Переключение MS Access / FEMSQ / JasperReports на `spMstrg_2606_ResultSet*`.
- `stNetDocBind` (настраиваемые привязки полей → `stcKey` вместо хардкода в триггерах).
- Постепенный отказ от прямого использования `ra_summCt` вне legacy (не блокер шага 1).
- Наполнение `ipgPnLim` / `ipgUtPlPnLmMn` для цепи 15 на продуктиве.

**Описания объектов БД (`MS_Description`):**

Все новые таблицы, столбцы, функции и триггеры пакета `_2606` снабжаются краткими описаниями через `sys.sp_addextendedproperty` (`MS_Description`). Они отображаются в SSMS и DBeaver (поле «Object description»). Паттерн — как в `code/config/sql/04_create_ra_f_tables.sql` и у существующих таблиц (`ra_summ` → «Суммы отчёта агента»).

**Дополнение к SQL-пакету (шаг 1):**

| Файл | Содержание |
|------|------------|
| `01b_CREATE_TABLE_factDoc.sql` | DDL `factDoc`, `factDocCost`, колонки `*_fdKey` |
| `01c_CREATE_TRIGGER_factDoc_sync.sql` | Триггеры на всех 6 подклассах |
| `01d_BACKFILL_factDoc.sql` | Миграция существующих данных |
| `03b0_CREATE_FUNCTION_fnStCost_2606.sql` | `fnStCostRa/RaCh/AgFee/Ralp/PrDoc/Mnrl_2606` |
| `07b_VERIFY_fnStCost_2606.sql` | Приёмка F: `fnStCost* = fnStCost*_2606` на выборке |

В `08_ROLLBACK.sql`: DROP триггеров, `fnStCost*_2606`, `factDocCost`, `factDoc`; колонки `*_fdKey` — по политике отката (опционально оставить NULL).

---

## Решение 10: Политика хранения `factDocCost` — вариант B+F

**Принято:** 2026-06-05 (подтверждено пользователем после проверки бэкфилла на FishEye dev).

### Контекст

После `01d_BACKFILL_factDoc.sql` проверена согласованность `factDocCost` с источниками `ra_summ` / `ra_summCt` (и аналогично `ra_change_summ` / `ra_change_summCt`). Вопрос: не произошло ли задвоение сумм при объединении плоских полей и Ct.

### Результаты проверки (FishEye dev)

| Проверка | Результат |
|----------|-----------|
| Плоские поля `ra_summ` → `factDocCost` (212/182/172/187) | **0** расхождений значений |
| Строки `ra_summCt` → `factDocCost` | **19 878 / 19 878** совпадают; **0** пропусков |
| Дубль одного `(fdcoFd, fdcoStCost)` из двух источников | **0** (`NOT EXISTS` + `UQ_factDocCost_fd_stcost`) |
| Наивный `SUM(factDocCost)` ≈ 2× `ras_total` | **~23 522** из 27 630 RA — ожидаемо: одновременно итог (212) и детализация (182/172/187) |

**Вывод:** построчного задвоения при переносе нет. «Раздувание» при наивном `SUM` — следствие иерархической модели хранения (итог + части), как в legacy `ra_summCt`.

### Вариант B — хранение

- **`01d` / `01c` не пересматриваем:** в `factDocCost` остаётся объединение плоских полей и дополнительных строк Ct (шаг 4).
- Триггер на `ra_summCt` **не добавляем** (шаг 2).
- Политики «только Ct» или «либо 212, либо 182/172/187» **не применяем**.

### Вариант F — приёмка функций `_2606`

Новые `fnStCost*_2606` **точно повторяют DAG-логику** соответствующих legacy-функций, но читают `factDocCost` вместо `ra_summCt` / плоских полей:

1. Найти «последний» носитель по дате (как в `fnStCostRa` / `fnStCostRaCh`).
2. Точное значение по `@StCostKey` в `factDocCost`.
3. Если NULL — `fnStDownAll` + исключение перекрытых узлов (копия алгоритма legacy).

**Критерий приёмки (тест F):**

```text
|fnStCost*(carrier, @StCostKey, @stNet) - fnStCost*_2606(...)| < 0.01
```

на выборке пар **(носитель, @StCostKey)**, где на **последней** сумме носителя есть строка `ra_summCt` / `ra_change_summCt` с этим `@StCostKey` (не декартово «любой Ct × все ключи 212/182/…»).

**Наивный `SUM(factDocCost)` по документу — не критерий приёмки.**

**Ожидаемое отличие (не регрессия):**
- RA **без** `ra_summCt` целиком, но с плоскими полями — `fnStCostRa` = 0, `fnStCostRa_2606` > 0.
- RA **с** Ct, но для конкретного `@StCostKey` только flat (напр. 182), а в Ct другой ключ (напр. 195) — legacy = 0, `_2606` > 0; такие пары **не входят** в тест F.

### Скрипт приёмки

`07b_VERIFY_fnStCost_2606.sql` (или блок в `07_VERIFY_after.sql`) — выполняется после `03b0` на dev.

---

## Решение 11: Контракт приёмки `_2606` ↔ `_2605` (цепь 5)

**Дата:** 2026-06-09 | **Обновлено:** 2026-06-09 (иерархия RS, `@MounthEndDate`)

### Контекст

Продуктив — только `_2605`. Эталон приёмки — **полный** `ResultSet1` / `fnIpgChRsltCstUtlPercentBrn_2605` (все `dateRslt` года, ~14 447 строк на цепи 5). Подробно: [`06-sp-recordsets-and-acceptance.md`](06-sp-recordsets-and-acceptance.md).

Стек `_2605` — **единая цепочка** (не параллельные стеки):

1. **fn2** — месячная матрица: факт без `ipgKey`; лимит per `ipgKey`; MONTH-фильтр — строки ИПГ в матрице.
2. **PercentBrn** — `@dt` / `dateRslt`: на переходе ИПГ одинаковый факт, разные lim/план → разный %.
3. **spMstrg** — RS1 = полный PercentBrn (Java, графики 01.01–31.12); RS4–RS7 = производные с `@MounthEndDate` (Access).

### Приоритет приёмки

| Приоритет | Объект | Критерий |
|-----------|--------|----------|
| **1** | **RS1** / полный PercentBrn | COUNT, все `dateRslt`, ключевые поля на переходах |
| 2 | fn2 | `presented` = `MAX(_2605)` по `(mNum, cstAgPnKey, iShKey)`; lim per ipgKey |
| 3 | RS4–RS7 | При `@MounthEndDate` — spot-check после совпадения RS1 |

`@MounthEndDate` **не** применяется к RS1; только к производным рекордсетам (текущий + 2 предыдущих месяца).

### Скрипты

- `07e_COMPARE_baseline_chain5.sql` — уровни A/B (fn2, модель PercentBrn); C — вспомогательный эталон @monthEnd.
- `07e1_DIAG_m9_presented_chain5.sql` — диагностика Level A FAIL (presented m9).
- `07f_COMPARE_PercentBrn_full_chain5.sql` — **TODO**, приоритет 1.

### Диагностика Level A (2026-06-09): разрыв источника факта RA

**Симптом:** 305 mismatches `presented` (в основном m9); `_2606=0`, `_2605` — миллиарды.

**Причина (не семантика ИПГ, не даты ipgChRl_2606):**

| Путь | Источник `presented` (RA) |
|------|---------------------------|
| `_2605` / `_2408` | `RRcTimeList.ras_total` + фильтр `complianceY` / `rsltOfConsider` |
| `fn2_2606` (mastering) | `fnMasteringPresRaMn` → **`fnStCostRa` (legacy)** по `ra_summCt` |

С **апреля 2022** у типичных строек `fnStCostRa(212)=0` при ненулевом `ras_total` в `RRcTimeList`. На m9: **291/291** ключей с `pres05>0` имеют `PresRaMn@30.09=0`. Spot (849): m1–m3 совпадают, m4–m12 — `_2408` миллиарды, `PresRaMn=0`.

**`fnStCostRa_2606` (factDoc)** близок к `_2408`, но **не эталон** `_2605` (849 m9: diff ~34.5 млн ₽). Для приёмки нужна логика **`_2408` / RRcTimeList**, не замена на `_2606` без сверки.

**Направление fix (фаза 1 fn2):** факт RA (`presented`, статусы) — из агрегации как в `_2408`; лимиты/план — mastering + `ipgChRl_2606`. Вариант 6А (`~25` fn) — после выравнивания fn2, с учётом того, что эталон — `_2408`, а не legacy `fnStCostRa`.

**Реализовано (2026-06-09):** CTE `raFact2408` + join в `base` (`04_CREATE_FUNCTION_fnIpgChRsltCstUtl2_2606.sql`, MSSQL2012/04). Точечная приёмка: `07e2_COMPARE_fn2_single_cstAgPn.sql` (`@cstAgPnKey`, опционально `@months`).

---

## Решение 12: `ras_work` → stCost **195**, не 182

**Дата:** 2026-06-13 | **Статус:** 📋 запланировано (этап 13 чат-плана)

### Контекст

При реализации Решения 9 в `01c`/`01d` плоское поле `ras_work` (колонка Excel **«СМР»**, `rainWork`) ошибочно маппилось на **182** (лист «Подрядные работы»). Канон RA в `ra_summCt`: **212 = 172 + 187 + 195**. Узел 182 — дочерний элемент 195.

Legacy `fnStCostRa` читает `ra_summCt` и при `@StCostKey=182` обычно возвращает 0 на верхнем уровне. `fnStCostRa_2606` через ошибочный `factDocCost@182` возвращает всю СМР — **8 674** регрессий на dev.

### Принято (целевая модель)

| Плоское поле | `fdcoStCost` |
|--------------|--------------|
| `*_total` | 212 |
| `*_work` | **195** |
| `*_equip` | 172 |
| `*_others` | 187 |

**182** в `factDocCost` — только из строк `ra_summCt` с `rscStCost=182`, не из триггера flat.

### Реализация

| Артефакт | Назначение |
|----------|------------|
| `01c` (правка) | Триггеры: набор 212/195/172/187 |
| `01d` (правка) | Бэкфилл для чистой установки |
| `01d1_FIX_factDocCost_ra_work_stCost.sql` | Миграция ~9,8k строк @182 |
| `07j` | Тест факта: pres@212 = pres@172+pres@187+pres@195 |
| Type 5 sanity §D | Gate после Excel apply |

**Полный план:** [`11-ra-work-stCost195-fix-plan.md`](11-ra-work-stCost195-fix-plan.md).

---

## Решение 13: К-7 строгая — только SQL-стек `_2606`

**Дата:** 2026-06-15 | **Статус:** 📋 принято (этап 14 чат-плана)

### Контекст

После П1–П4 и v9.0: fn2 stIpg=46 **49 с** ✅ (К-6), fn2 stIpg=NULL **279,8 с** ❌ (К-7 строгая <120 с).
Клиент — **MS Access**; бизнес-процесс требует **многократного пересчёта в течение дня** после загрузки Excel.

### Отклонённые пути

| Путь | Причина |
|------|---------|
| App-level parallelism (N вызовов fn2 по stIpg) | MS Access не поддерживает параллельные вызовы |
| Persistent cache mastering | устаревание при каждом Excel-импорте |
| Смягчение К-7 («прагматик» <5 мин) | `_2605` должен обеспечивать **интерактив** |

### Принято

Закрытие **К-7 строгой** — **только** совершенствование процедур и функций стека `_2606`:

1. **Ступень 1** — индексы FK (`00-perf-indexes-k7.sql`): `ra.ra_cac`, `ralp`, `cn_PrDocP`, …
2. **Ступень 2** — `@ralpCostBase` + `@prDocMnrlCostBase` в `fnMasteringCstAgPn_2606` (P6-lite, паттерн 9б)
3. **Ступень 3** — fn2 MSTVF → SP с `#schemeRows` + индекс (если после 1+2 > 90 с)

**Критерий приёмки:** `07h6` fn2 stIpg=NULL **< 120 000 ms**; регрессия `07h` stIpg=46/61 и `07f` — без ухудшений.

**Детали:** `07-performance-analysis.md` §8–10.

---

## Решение 14: План на датах смены ИП — «ужесточение» `_2605`; интерполяция — отложенная задача

**Дата:** 2026-06-23 | **Статус:** ✅ принято (политика приёмки); 📋 отложено (интерполяция плана и факта по дням)

### Контекст

В стеке `_2605` актуальность ИП определяется по **месяцу**; накопленный план на месяц смены включает **целый** месячный прирост UtPl. В `_2606` актуальность — по **дню** (`ipgChRl_2606`, `fnIpgChDats_2606`), но `fnStCostRsIpgPn_2606` по-прежнему накапливает **полные** месячные `iuplpmLim` в `smmTtl` на датах `ipgcrvEnd` / `ipgcrvStr` (parity с «ужесточением» `_2605`).

Эффект: при смене ИП в середине месяца (например, 26–27.04) исполнитель формально «должен» освоить план за большую долю месяца, чем календарные дни активности ИП. Аналогичный сдвиг возможен и для **факта**, если документы засчитываются по датам периода без пропорции к промежуточному дню.

### Принято (текущая реализация и приёмка)

1. **Сохранить** «ужесточение» из `_2605` в `_2606` — без изменения `03a` / mastering до отдельного решения.
2. Критерии приёмки планов (**К-12c**, **К-12t**): на `ipgcrvEnd` cum(UtPl) **без** пропорции по дню месяца.
3. Термин **«ревизия»** не применять к смене ИП; см. `docs/project/glossary.md`.

### Отложенная задача (объёмная, возможно позже)

Для **корректной** работы на промежуточных днях месяца потребуется:

| Контур | Требование |
|--------|------------|
| **План** | Интерполяция накопленного плана по дням в месяце смены ИП (пропорция активных дней или иная согласованная модель). |
| **Факт** | Определение по **датам из документов факта**, что именно принято/представлено к промежуточному дню (симметричный cutoff с планом). |

Затрагивает: `fnStCostRsIpgPn_2606`, функции mastering факта, возможно модель UtPl и клиентский отчёт. **Не блокер** релиза SQL-пакета `_2606` при текущей политике.

**Связанные этапы чат-плана:** 18.9 (проектирование), реализация — после 18.7.4.

---

## Решение 15: Sparse-хранение UtPl и CHECK `lim > 0`

**Дата:** 2026-06-26 | **Статус:** ✅ принято (политика данных, fixture, deploy)

### Контекст

1. Таблицы нормализованной разбивки UtPl (`ipgUtPlPnLmMn`, `ipgUtPlPnLmQu`, `ipgUtPlPnLmYe`) изначально снабжены CHECK `iuplp*Lim <> 0`, но ограничения **отключены** (DISABLED / NOT TRUSTED).
2. Dev-fixture (FIXTURE_05/06) вставляли полную сетку 12 месяцев × 4 stCost с нулями в «пустых» месяцах — **не соответствует** продуктивной семантике и раздувает объём данных.
3. Стек `_2606` (`fnStCostUtPlPMn`) при отсутствии строки на месяц возвращает `0`; явные нулевые строки **избыточны** для расчёта и приёмки К-12/К-13.
4. Аудит dev (18.8.0): prod-подобные ~45k строк LmMn уже sparse; нарушения — 888 нулей (fixture) и ~10 строк `lim < 0` (legacy, округление mn=12 / Q4 @212).

### Принято

#### Модель данных (sparse)

| Утверждение | Деталь |
|-------------|--------|
| Хранить только `lim > 0` | Строка = «в этом периоде **есть** плановое освоение» |
| Нет плана в месяце | **Нет строки** в `ipgUtPlPnLmMn` (не `iuplpmLim = 0`) |
| Каноническая таблица stCost | `ipgUtPlPnLmMn` (не `ipgUtPlPnCostIts`) |
| Сверка лимита | `SUM(iuplpmLim)` по существующим месяцам ≈ `ipgPnLim` / `ipgpSm*` (К-12) |

#### Fixture и dev-приёмка

- INSERT только при `iuplpmLim > 0`; verify-скрипты: `COUNT(lim=0)=0` на данных fixture.
- Отказ от `CROSS JOIN` 12×4 с нулевым заполнением (этап 18.8.2).

#### Миграция и CHECK

| Шаг | Содержание |
|-----|------------|
| **09a** | Аудит `lim <= 0` (отчёт) |
| **09b** | Sparse-очистка `lim <= 0`: нули — `DELETE`; отрицательные — `DELETE` **с компенсацией** (вычесть \|lim\| из другой строки того же plPn+stCost, типично mn=11 / пред. квартал), чтобы **SUM не изменился** |
| **09c** | DROP legacy `CK_*<>0`, ADD `CK_*_gt0` (`lim > 0`) WITH CHECK |

**Порядок на dev:** 18.8.2 (sparse fixture) → 18.8.3 (очистка) → 09a → 09b? → **09c**.  
**Порядок на prod (deploy-day):** DDL `00`…`08` → **09a → 09b → 09c** (п. 17.3.1) → приёмка.

**Отрицательные `lim` (legacy):** артефакты округления (mn=12 / Q4 @212), **уравнивали** `SUM(месяцев)` с лимитом. При sparse-очистке их нельзя просто удалять — нужна **компенсация** в другой строке (см. `09b`). Наивный `DELETE` без компенсации увеличивает SUM и нарушает К-12.

**Допущение prod:** с момента отпочкования dev от prod новых ценных UtPl для `_2606` не вносилось.

#### Вне scope Решения 15

- Широкие колонки `ipgUtPlP.iuplpM01`…`M12` (семантика итога **stCost 212**, без колонки stCost) — legacy UI/`_2605`; для `_2606` см. **Решение 22**.
- Удаление `ipgUtPlPnCostIts` — по отдельному решению (см. glossary).

### Критерии приёмки Решения 15

1. На dev после 18.8.4: `SELECT COUNT(*) … WHERE lim<=0` = **0**; три CHECK **ENABLED** + WITH CHECK.
2. Fixture verify: нулевых строк на данных fixture нет.
3. 07n/07o на 9 пилотных cst — **PASS** (18.8.5).
4. Флешка содержит `09a`–`09c`; deploy-checklist обновлён (18.8.6).

**Связанные документы:** `docs/project/glossary.md`, `13-plan-stcost-monthly-acceptance.md` §16, чат-план 18.8.

---

## Решение 16: Универсум строек в рекордсете — `stIpgOutLimPn_2606` и фильтр `fn2`

**Проблема:** при `@ipgStKey ≠ NULL` стек mastering (`fnMasteringStIpgStCost_2606`) отбирает стройки по `ipgStPn`, но `fn2_2606` → `PercentBrn` → `spMstrg_2606` строит сетку контрактов по **всей цепи** (наследие `fn_2408`). В RS остаются сотни «чужих» строек. В `_2605` сужение делалось через `importIpgSt_26-0320`; в `_2606` эквивалент не реализован. Справочник `ipgStLm` / колонка `stiLim` **не участвуют** в расчётах.

**Принято (2026-06-24):** двухкомпонентный универсум строек для отчёта при заданном `@ipgStKey`:

| Компонент | Определение |
|-----------|-------------|
| **IN_GROUP** | `cstAgPn`, у которых хотя бы один `ipgpPn` привязан к `@ipgStKey` или потомку через `ipgStPn` (`fnStDownAll` + корень) — как в `fnMasteringStIpgStCost_2606` |
| **OUT_GROUP** | стройки **вне** IN_GROUP, у которых (1) тип по **5-й литере** кода САК разрешён в `stIpgOutLimPn_2606` для `@ipgStKey`, (2) стройка ∈ **`chainActive`** (см. ниже) |

**Дополнение (2026-06-29) — предикат активности OUT_GROUP:**

Стройка считается **активной** для OUT_GROUP, если она входит в **`chainActive`** — тот же `UNION DISTINCT` семи источников, что CTE `ipgChContracts` в `fn2_2606` и `fnIpgChRsltCst` в `_2605` / `_2408`:

- `ogAgFee` / `ogAgFeeP`, RA (`RRcTimeList`), RALP, `cn_PrDocP` (хранение/ССК), стройки из **`ipgPn` цепи**, `cstAgPnMnrl`.

Отдельного фильтра `lim > 0`, `accepted > 0` или `limNot`/`limOver` на уровне `fnIpgChContractsForStIpg_2606` **нет** — **паритет** с полным перечнем `_2605` до сужения по `importIpgSt`. В перечень OUT попадают в том числе стройки из `ipgPn` цепи без факта в отчётном году; строки без данных отсекает **существующий** NOT-all-NULL фильтр в `fn2` (как в `_2605`).

**Отложено (не в scope этапа 19):** ужесточение OUT до «только с фактом» или «только с lim > 0» — отдельное решение с пересчётом приёмки `07q`.

**Два независимых слоя:**

| Слой | Вопрос | Механизм |
|------|--------|----------|
| Универсум строек | какие `cstAgPn` в RS при `@ipgStKey`? | IN_GROUP ∪ OUT_GROUP (`fnIpgChContractsForStIpg_2606` → фильтр `ipgChContracts` в fn2) |
| Раскладка по схемам | куда факт на строке отчёта? | `typeGrTtl` → `ag_*` / `np_*` / `oh_*` (без изменений; см. `09-scheme-cascade-mastering.md`) |

Фильтр `stIpg` **не меняет** распределение по `np_` / `oh_`.

**Тип стройки (5-я литера кода САК):**

| Символ | Смысл | Пример |
|--------|-------|--------|
| `1` | ПИР | `051-1001234` |
| `2` | стройка (КС) | `051-2001234` |
| `3` | бурение | `051-3001234` |

Извлечение: `SUBSTRING(cstapIpgPnN, 5, 1)` — единая функция `ags.fnCstAgPnTypeChar`.

**Хранение правил OUT_GROUP — таблица `ags.stIpgOutLimPn_2606` (1→N):**

```text
siolpStIpg    int      FK → stIpg.stiKey
siolpCstType  char(1)  CHECK IN ('1','2','3')
PK (siolpStIpg, siolpCstType)
```

| `stiKey` | Строки `siolpCstType` | OUT_GROUP |
|----------|------------------------|-----------|
| **1** | `1`, `2`, `3` | все внелимитные с активностью |
| **51** | `2`, `3` | стройка + бурение |
| **45** | `1` | ПИР |
| **42, 61**, листья | *(нет строк)* | только IN_GROUP |

Узел **2** («План кап. строительства») при необходимости — те же три строки, что у узла **1**.

**`stiLim` / `ipgStLm`:** в runtime **не использовать**; источник истины — только `stIpgOutLimPn_2606`. Пустая таблица для узла = нет OUT_GROUP.

**Реализация в `_2606` (этап 19 чат-плана):**

1. DDL + seed `stIpgOutLimPn_2606`; `fnCstAgPnTypeChar`. ✅ 19.1
2. `fnIpgChContractsForStIpg_2606(@ipgCh, @ipgStKey)` → множество `cstAgPn`. ✅ 19.2
3. Фильтр CTE `ipgChContracts` (и при необходимости `nullIpgBase`) в `fn2_2606`. ✅ 19.3
4. `@ipgStKey = NULL` — без изменений (вся `chainActive`).
5. `fnMasteringStIpgStCost_2606` — IN_GROUP без смены модели; внелимитные в RS — из `chainActive`, план/лимит по `ipgPn` часто NULL; в PercentBrn остаются строки с ненулевым фактом или лимитом (NOT-all-NULL в fn2).

**Контрольные COUNT для приёмки** (`fnIpgChContractsForStIpg_2606`, цепь **5**, dev @ `10.7.0.3`, smoke `10d`):

| `@ipgStKey` | DISTINCT `cstAgPn` | Комментарий |
|-------------|---------------------|-------------|
| `NULL` | **968** | вся `chainActive` |
| **1** | **968** | корень + OUT всех типов |
| **51** | **967** | OUT: типы `2`,`3` |
| **45** | **0** | OUT: только ПИР; в цепи нет |
| **42**, **61** | **1** | только IN (`cstAgPn` 2102); лист без строк OUT |

**Связанные документы:** `14-stipg-stilim-contract-universe-proposals.md`, `09-scheme-cascade-mastering.md`, `glossary.md`, чат-план **этап 19**.

---

## Решение 17: Единый календарь RS1 — 17 контрольных дат (`fnIpgChDats_2606`)

**Проблема:** графики Java/FEMSQ заявлены на интервал **01.01–31.12** расчётного года. На **01.01** должна быть начальная точка: план = 0, факт = 0, «не освоено» ≈ лимит. В стеке `_2606` **`fnIpgChDats_2606`** и mastering уже работают на **17 датах** (01.01 + 12 EOMONTH + переходы `ipgChRl_2606`), но **`fnIpgChRsltCstUtlPercentBrn_2606`** унаследован от `_2605` и строит календарь `@dt` **без 01.01**. Позднее в legacy PercentBrn (`_2408` / `_2605`) 01.01 «привинчивали» через UNION с **`mNum = 0`** в подзапросе планов — не в едином календаре `@dt`. На dev (цепь 5, 2022) в RS1 наблюдается **16** distinct `dateRslt` и ~14 447 строк — **расхождение с целевым контрактом**, а не «два календаря по замыслу».

**Принято (2026-06-29):**

| Аспект | Legacy `_2605` / текущий PercentBrn_2606 | Целевой `_2606` |
|--------|------------------------------------------|-----------------|
| Календарь `@dt` / `dateRslt` | `@dt` из `ipgStr`/`ipgEnd`/EOMONTH; костыль 01.01 + `mNum=0` | **`fnIpgChDats_2606(@ipgCh)`** + привязка `ipgKey` по **`ipgChRl_2606`** |
| Число дат (цепь 5, 2022) | **16** в RS1 | **17** (= mastering) |
| Начальная точка графика | отсутствует или через `mNum=0` | **2022-01-01**: `ag_Pl`/`ag_PlAccum` = 0, `ag_restOfLimit` ≈ `ag_lim` |
| Семантика среза | помесячная матрица fn2 + фиктивный месяц 0 | накопления «на дату» согласованные с mastering |

**Оптимальная точка внедрения:** замена построения `@dt` в **`fnIpgChRsltCstUtlPercentBrn_2606`** (`05_CREATE_FUNCTION_…`). **Не** в fn2 (помесячная матрица mNum 1…12). **Не** только в mastering при несогласованном RS1. **Не** в spMstrg (материализация готового набора).

**Убрать при рефакторе:** UNION `DATEFROMPARTS(@lsYy, 1, 1)` с `IIF(ddd < 31.01, 0, MONTH(ddd))` в plan-JOIN; legacy `@dt` без 01.01.

**Два независимых gate приёмки** (см. `06-sp-recordsets-and-acceptance.md`):

| Gate | Вопрос | Критерий | Когда |
|------|--------|----------|-------|
| **parity** | Этап 19 / прочие правки не сломали legacy? | RS1 `_2606` = `_2605` построчно (вкл. `rowNum`, `limSort`); **16** `dateRslt` как у prod | до флешки, скрипт `07s` |
| **calendar** | RS1 соответствует контракту графика? | `fnIpgChDats_2606 EXCEPT dateRslt(RS1)` = ∅; на **01.01** plan=0, rest≈lim; spot 2102: **68** строк (= 17×4 GROUPING SETS) | после рефакторе `05` |

Допустимо: **parity PASS** и **calendar FAIL** до исправления PercentBrn — ожидаемое состояние.

**Влияние на COUNT:** полный RS1 цепи 5 вырастет с ~14 447 (16 дат) до **большего** числа (17 дат × стройки × 4 уровня GROUPING SETS). Эталон `07f`/`07k` пересчитывается после fix. Паритет с `_2605` на prod **сохраняется** до отдельного релиза исправления PercentBrn на prod.

**Связанный дефект (вне scope Решения 17):** «фантомный» уровень филиала в GROUPING SETS при `branch=0` — дубль агента в RS1 (в RS4–RS7 отфильтрован). Устранение — отдельное решение (этап 20.6 чат-плана).

**Реализация (этап 20 чат-плана):** документация ✅ → `07s` parity → рефактор `05` → `07s` calendar → пересчёт `07f`/`07k` → sync `MSSQL2012/05` → **19.7** флешка.

**Связанные документы:** `04-computation-map.md` §Шаг 0, `06-sp-recordsets-and-acceptance.md`, `glossary.md` (календарь RS1, начальная точка графика), `07f1_diagnose_dt_cac371.sql`, `07f2_trace_eomonth_join.sql`.

---

## Решение 21: Конвенция имён стека `_2606` — `ipgChRl_2606`, единый источник плана, agency-golden

**Дата:** 2026-06-30  
**Контекст:** ручные прогоны `spMstrg_2606` на golden **2102** (`stIpg=42`) выявили ограничения для приёмки; анализ именования стека и расхождения PercentBrn с mastering.

### Проблема 1 — двусмысленность имён

| Объект | Суффикс сегодня | Смысл | Путаница |
|--------|-----------------|-------|----------|
| `fnMastering*_2606`, `spMstrg_2606` | `_2606` | версия алгоритма / стека | — |
| `ipgChRl_2606`, `fnIpgChDats_2606` | `V` | validity (срок в цепи) | не читается как пара к `ipgChRl` |
| `ipgChRl` | без суффикса | legacy-связь цепь↔ИП (prod) | смешивается с «новой» связью |

Стек `_2606` читает сроки актуальности из **`ipgChRl_2606`**, а **`fnIpgChRsltCstUtlPercentBrn_2606`** в plan-JOIN по-прежнему использует legacy **`ipgChRl`** (`ipgcrUtPlGr` → группы **3/4/6**). Mastering и скрипты приёмки (`07n`/`07o`) — через **`ipgChRl_2606`** и тестовые группы **18/19/20** (FIXTURE_06). Итог: план в RS1 на ИП 8/11 по **2102** = NULL при наличии UtPl в fixture.

### Проблема 2 — golden 2102 не покрывает agency-приёмку

| Критерий | `cstAgPn=2102`, `stIpg=42` | Нужно для тестов |
|----------|----------------------------|------------------|
| Схема | только инвестиционная (`ipgpSh=1`) | агентская (`ipgpSh=2`) |
| Факт освоения | `presented=0` в fn2 | осязаемый факт по месяцам |
| План при смене ИП | `iv_Pl` только на ИП 6 (legacy join) | помесячный/накопленный план на ИП 6/8/11 |
| Две стройки в одном `stIpg` | одна стройка | пара agency в одном узле |

**Принято:** ввести **agency-golden** (`stIpg=4`, **849** + **1862**); **2102** оставить для invest-golden (календарь, лист `stIpg`, инвест-колонки).

### Проблема 3 — группы UtPl 3/4/6 vs 18/19/20

Группы **3/4/6** — prod-подобные планы («январь/май/октябрь 2022»), **не изменять** в fixture. Группы **18/19/20** (`TEST_CH5_IPG6/8/11`) — изолированный тестовый контур FIXTURE_06; fixture и приёмка **только** в них. Дублировать план в 3/4/6 **запрещено**.

---

### Принято: конвенция имён

| Категория | Правило | Примеры |
|-----------|---------|---------|
| FUNCTION / PROCEDURE стека | суффикс **`_2606`** | `fn2_2606`, `spMstrg_2606` |
| TABLE-заменитель legacy | **`{legacyName}_2606`** | `ipgChRl` → **`ipgChRl_2606`** |
| Legacy prod | **без суффикса**, только чтение где неизбежно | `ipgChRl`, `ipgPn` |
| Validity-колонки | префикс **`ipgcrv*`** сохраняется | независимо от имени таблицы |
| Платформенный DDL | **без** `_2606` | `factDoc`, `factDocCost` (Решение 9) |
| Dev-fixture группы | имена `TEST_*`, не трогать prod-группы | `ipgUtPlGr` 18–20 |

**Переименование объектов (этап 21.1):**

| Было | Стало |
|------|-------|
| `ags.ipgChRl_2606` | `ags.ipgChRl_2606` |
| `ags.fnIpgChRlEnd_2606` | `ags.fnIpgChRlEnd_2606` |
| `ags.fnIpgChDats_2606` | `ags.fnIpgChDats_2606` |
| `ags.stIpgOutLimPn_2606` | `ags.stIpgOutLimPn_2606` |

`fnCstAgPnTypeChar` — **без** переименования (утилита; опционально `_2606` при появлении второго потребителя).

**Обратная совместимость:** synonym `ipgChRl_2606` **не создавать** — одно каноническое имя на prod и dev.

---

### Принято: PercentBrn — единый источник плана

В подзапросах `gap` / `gip` / `gup` функции **`fnIpgChRsltCstUtlPercentBrn_2606`** заменить `ipgChRl` на **`ipgChRl_2606`** (после переименования таблицы). Календарь `@dt` — **`fnIpgChDats_2606`** (уже из Решения 17; только rename).

**Критерий приёмки (spot 2102, `stIpg=42`):** `iv_Pl` / `iv_PlAccum` **ненулевые** на ИП **6, 8, 11** там, где FIXTURE_06 создал UtPl в группах 18–20.

---

### Принято: agency-golden и fixture

| Роль | Узел | Стройки | Назначение |
|------|------|---------|------------|
| invest-golden | `stIpg=42` (лист) | **2102** | календарь 17 дат, `stIpgOutLimPn_2606`, инвест-колонки |
| agency-golden | `stIpg=4` | **849**, **1862** | факт + план на ИП 6/8/11; две agency-стройки в одном узле |

**FIXTURE_08** (dev-only): sparse UtPl @212/195/172/187 для agency-строек **только** в группах **18–20**, лимиты от `ipgPn` / `ipgPnLim`; предварительно — прогон **К-12** на **849** (нативный UtPl может быть достаточен).

---

### Gate приёмки этапа 21

| Gate | Критерий |
|------|----------|
| **naming** | `08_ROLLBACK` + `07_VERIFY_after` — объекты под новыми именами; в SQL-пакете нет ссылок на `ipgChRl_2606` / `fnIpgChDats_2606` (кроме комментариев «было») |
| **plan-align** | 2102: `iv_Pl` на ИП 8/11; `07o` / spot PercentBrn согласованы с mastering |
| **agency-spot** | `@ipgStKey=4`: `ag_Pl` и `ag_accepted` ненулевые на ИП 6/8/11 для 849 (и при необходимости 1862) |

Паритет RS1 с `_2605` (**gate parity**, Решение 17) — **не регрессировать** на общих 16 датах; изменение plan-JOIN может сдвинуть `_2606` относительно `_2605` на датах с планом — зафиксировать в `07s` и принять как **ожидаемое улучшение** после согласования.

---

### Порядок работ (этап 21 чат-плана)

1. **21.0** — обновление документации (до любого DDL rename).
2. **21.1** — переименование объектов + массовая замена в пакете `26-0604` / `MSSQL2012/`.
3. **21.2** — патч PercentBrn: plan-JOIN → `ipgChRl_2606`.
4. **21.3** — agency-golden: документация приёмки, FIXTURE_08 (при необходимости), spot `stIpg=4`.
5. **21.4** — gate naming + plan-align + agency-spot; обновить `07_VERIFY_*`.
6. **21.5** — sync `MSSQL2012/`, **19.7** флешка (после PASS 21.4).

**Зависимости:** **19.7** и **17.2.2b** — **после** этапа **21** (не до naming). **20.6** (фантомный филиал) — по-прежнему независимо.

**Связанные документы:** `00-overview.md`, `04-computation-map.md`, `06-sp-recordsets-and-acceptance.md`, `09-scheme-cascade-mastering.md`, `13-plan-stcost-monthly-acceptance.md`, `glossary.md`, `db-upgrade-spMstrg-2606.md`, FIXTURE_06 README, чат-план **этап 21**.

---

## Решение 22: Источник плановых колонок PercentBrn — `ipgUtPlPnLmMn`, не `iuplpM*`

**Дата:** 2026-07-06  
**Контекст:** диагностика **21.4.2** — plan-align FAIL при PASS К-12/07n; расхождение mastering и RS1 по `ag_Pl`/`iv_Pl`.

### Модель данных (утверждённая семантика)

| Слой | Таблица / поля | stCost | Назначение |
|------|----------------|--------|------------|
| Заголовок UtPl | `ipgUtPlP` (`iuplpKey`, `iuplpIpgPn`, `iuplpPl`, `iuplpLim`) | — | якорь LmMn; связь `ipgPn` ↔ группа планов |
| Широкий помесячный план | `ipgUtPlP.iuplpM01`…`M12`, `M*Accum` | **нет колонки**; по смыслу **итог (212)** | legacy UI / `_2605` PercentBrn |
| Нормализованный план | `ipgUtPlPnLmMn` | **да** (212/195/172/187…) | канон для `_2606`, sparse (Решение 15) |

`ipgUtPlPnLmMn` связана с пунктом ИП через **`ipgUtPlP.iuplpKey`** (`iuplpmPlPn`), не напрямую с `ipgPn`. Прямая связь LmMn→`ipgPn` **не требуется** для `_2606`: группа планов на ИП задаётся `ipgChRl_2606.ipgcrvUtPlGr` + выбор «активного» `ipgUtPlP` (как в `07n`).

### Проблема — два контура чтения плана

| Контур | Источник | Статус на dev (gr 18–20) |
|--------|----------|--------------------------|
| Mastering, `fnStCostRsIpgPn_2606`, К-12 | `fnStCostUtPlPMn` → **LmMn** | ✅ |
| PercentBrn `ag_Pl`/`iv_Pl` | подзапрос gap/gip → **`iuplpM*`** | ❌ при пустых `M*` |

На prod legacy-группы UtPl **3/4/6** часто имеют заполненные `iuplpM*` (другой `iuplpPl` на тот же `ipgPn`); `_2605` PercentBrn работает. После FIXTURE_06 (тестовые gr **18–20**) LmMn заполнен, `iuplpM*` — нет → ложное ощущение «план есть, RS пустой».

### Принято

1. **Для стека `_2606` каноническим источником плана в расчётах** (включая PercentBrn после этапа **21.4.3**) является **`ipgUtPlPnLmMn`** по тому же пути, что mastering (`fnStCostRsIpgPn_2606` / cumulative @ stCost **212** для колонок `ag_Pl`/`iv_Pl` в RS).
2. **`iuplpM*` не синхронизировать** в fixture как долгосрочное решение (FIXTURE_09 backfill — только временный костыль, **не** принято).
3. **Стек `_2605` и prod PercentBrn** до отдельного релиза **не менять**; расхождение `_2606` ↔ `_2605` по полям плана на общих датах после plan-align — **ожидаемо** (Решение 21).
4. **Gate `07o`:** критерий «`iv_Pl` на ИП 6, 8, 11 одновременно на yearend» **пересмотреть** — на `2022-12-31` активна одна ИП в цепи (смена актуальности ИП, не ревизия факта); проверять план **по календарю `dateRslt`** и активной ИП на дате (как `07t`, К-12r в `07n`).

### Вне scope Решения 22

- Реорганизация FK (LmMn → `ipgPn` напрямую).
- Удаление колонок `iuplpM*` из БД / смена UI Access.
- Синхронизация og / `iuplpSubAg` на заголовке UtPl.

### Порядок реализации (чат-план)

| # | Задача |
|---|--------|
| **21.4.3** | Рефактор plan-колонок `fnIpgChRsltCstUtlPercentBrn_2606`; dev apply; refill RS1 |
| **21.4.4** | Gate plan-align PASS; уточнённый `07o` |
| **21.4.5–21.4.7** | agency-spot, parity/calendar, оркестратор |

**Связанные документы:** `glossary.md` (широкие месячные колонки, помесячный план по stCost), `04-computation-map.md`, `diag-21_4_2-plan-align-findings.md`, `13-plan-stcost-monthly-acceptance.md`.

---

*Файл создан: 2026-06-04. Решение 7 пересмотрено: 2026-06-04. Решение 8: 2026-06-04. Решение 9: 2026-06-05. Решение 10 (B+F): 2026-06-05. Решение 11: 2026-06-09. Решение 12: 2026-06-13. Решение 13: 2026-06-15. Решение 14: 2026-06-23. Решение 15: 2026-06-26. Решение 16: 2026-06-24; дополнение (предикат активности, приёмка): 2026-06-29. Решение 17: 2026-06-29. Решение 21: 2026-06-30. Решение 22: 2026-07-06.*
