# Анализ существующих отчётов FEMSQ

**Дата создания:** 2025-12-05  
**Автор:** Александр  
**Этап:** 08.4 - Изучение примеров существующих отчётов в FEMSQ  
**Связанный план:** `docs/development/notes/chats/chat-plan/chat-plan-25-1205-jasper-report-mstrg.md`

---

## 1. Обзор найденных отчётов

### 1.1 Список отчётов

| Отчёт | Файл | Тип | Назначение |
|-------|------|-----|------------|
| **my-new-report** | `my-new-report.jrxml` | Основной | Карточка организации с подотчётом |
| **my-new-report-sr-ag** | `my-new-report-sr-ag.jrxml` | Подотчёт | Список кодов агента для организации |

**Расположение:**
```
code/femsq-backend/femsq-reports/src/main/resources/reports/embedded/
├── my-new-report.jrxml          (90 строк)
├── my-new-report.json            (39 строк)
├── my-new-report.jasper          (скомпилированный)
├── my-new-report-sr-ag.jrxml     (62 строки)
└── my-new-report-sr-ag.jasper     (скомпилированный)
```

---

## 2. Анализ основного отчёта (my-new-report.jrxml)

### 2.1 Настройки страницы

```xml
<jasperReport 
  name="my-new-report" 
  language="java" 
  pageWidth="842" 
  pageHeight="595" 
  orientation="Landscape" 
  columnWidth="802" 
  leftMargin="20" 
  rightMargin="20" 
  topMargin="20" 
  bottomMargin="20">
```

**Характеристики:**
- **Формат:** A4 Landscape (842 x 595 points = 297 x 210 mm)
- **Поля:** 20 points (≈7 mm) со всех сторон
- **Ширина колонки:** 802 points (297 - 14 = 283 mm)
- **Ориентация:** Landscape (альбомная)

**Сравнение с нашим отчётом:**
| Параметр | my-new-report | mstrgAg_23_Branch_q2m_2408_25 |
|----------|---------------|-------------------------------|
| Формат | A4 Landscape | **A3 Landscape** |
| pageWidth | 842 | **1191** |
| pageHeight | 595 | **842** |
| Поля | 20 | **14** (5 mm) |
| columnWidth | 802 | **1163** |

### 2.2 Параметры отчёта

```xml
<parameter name="SCHEMA_NAME" class="java.lang.String">
  <defaultValueExpression><![CDATA["ags"]]></defaultValueExpression>
</parameter>

<parameter name="contractorId" class="java.lang.String">
  <defaultValueExpression><![CDATA["71"]]></defaultValueExpression>
</parameter>

<parameter name="SUBREPORT_DIR" class="java.lang.String">
  <defaultValueExpression><![CDATA["./src/main/resources/reports/embedded/"]]></defaultValueExpression>
</parameter>
```

**Паттерны:**
1. **SCHEMA_NAME** - обязательный параметр для динамической схемы БД
   - Тип: `String`
   - Значение по умолчанию: `"ags"`
   - Использование: `$P!{SCHEMA_NAME}` в SQL (динамическая подстановка)

2. **contractorId** - бизнес-параметр (ID организации)
   - Тип: `String` (хотя в БД это INT)
   - Значение по умолчанию: `"71"` (для тестирования)
   - Использование: `$P{contractorId}` в SQL

3. **SUBREPORT_DIR** - путь к подотчётам
   - Тип: `String`
   - Значение: относительный путь к каталогу с отчётами
   - Использование: для подключения подотчётов

**Рекомендации для нашего отчёта:**
- ✅ Использовать `SCHEMA_NAME` параметр (уже добавлен)
- ✅ Использовать `SUBREPORT_DIR` если будут подотчёты (пока не требуется)
- ✅ Бизнес-параметры: `ipgCh` (Integer), `MounthEndDate` (String/Date)

### 2.3 SQL запрос

```xml
<query language="sql"><![CDATA[
SELECT 
  og.ogKey,
  og.ogNm AS ogName,
  og.ogNmFl AS fullName,
  og.ogRgTaxType AS ogType
FROM $P!{SCHEMA_NAME}.og og
WHERE og.ogKey = CAST($P{contractorId} AS INT)
]]></query>
```

**Паттерны:**
1. **Динамическая схема:** `$P!{SCHEMA_NAME}.og`
   - `$P!{}` - подстановка без кавычек (для имён таблиц/схем)
   - `$P{}` - подстановка с кавычками (для значений)

2. **Алиасы полей:** `og.ogNm AS ogName`
   - Использование алиасов для читаемости
   - Соответствие Java naming conventions

3. **Приведение типов:** `CAST($P{contractorId} AS INT)`
   - Параметр String, но в БД INT
   - Явное приведение типа в SQL

**Рекомендации для нашего отчёта:**
- ✅ Использовать `$P!{SCHEMA_NAME}` для схемы (уже используется)
- ✅ Алиасы не требуются (все поля уже имеют читаемые имена)
- ✅ ORDER BY уже добавлен

### 2.4 Определение полей (fields)

```xml
<field name="ogKey" class="java.lang.Integer">
  <property name="com.jaspersoft.studio.field.name" value="ogKey"/>
  <property name="com.jaspersoft.studio.field.label" value="ogKey"/>
</field>

<field name="ogName" class="java.lang.String">
  <property name="com.jaspersoft.studio.field.name" value="ogName"/>
  <property name="com.jaspersoft.studio.field.label" value="ogName"/>
</field>
```

**Паттерны:**
1. **Типы данных:**
   - `java.lang.Integer` - для целых чисел
   - `java.lang.String` - для текста
   - `java.math.BigDecimal` - для денег (в нашем отчёте)
   - `java.lang.Double` - для процентов (в нашем отчёте)

2. **Свойства полей:**
   - `com.jaspersoft.studio.field.name` - техническое имя
   - `com.jaspersoft.studio.field.label` - отображаемое имя

**Рекомендации для нашего отчёта:**
- ✅ Использовать правильные типы (BigDecimal для money, Double для float)
- ✅ Свойства полей опциональны (можно не добавлять)

### 2.5 Структура bands (секций)

#### 2.5.1 Title (Заголовок отчёта)

```xml
<title height="32" splitType="Stretch">
  <band height="32">
    <element kind="staticText" 
             uuid="eb494bb7-30b7-4e6c-baf0-04435fb08464" 
             x="0" y="0" width="280" height="30" 
             fontName="DejaVu Sans" 
             pdfFontName="DejaVu Sans">
      <text><![CDATA[Мой новый отчёт]]></text>
    </element>
  </band>
</title>
```

**Паттерны:**
- **Высота:** 32 points (≈11 mm)
- **Элемент:** `staticText` (статический текст)
- **Шрифт:** DejaVu Sans (поддержка кириллицы)
- **UUID:** уникальный идентификатор элемента

#### 2.5.2 Detail (Строки данных)

```xml
<detail>
  <band height="235" splitType="Stretch">
    <!-- Поля данных -->
    <element kind="textField" 
             uuid="949a869e-baf2-4e4a-b2b3-d175bd037c08" 
             x="80" y="8" width="100" height="30" 
             blankWhenNull="true">
      <expression><![CDATA[$F{ogKey}]]></expression>
    </element>
    
    <!-- Подотчёт -->
    <element kind="subreport" 
             uuid="eaaeacbb-f7c6-4f2e-8534-3d9d6dcf9829" 
             x="0" y="140" width="760" height="90">
      <connectionExpression><![CDATA[$P{REPORT_CONNECTION}]]></connectionExpression>
      <expression><![CDATA[$P{SUBREPORT_DIR} + "my-new-report-sr-ag.jasper"]]></expression>
      <parameter name="SCHEMA_NAME">
        <expression><![CDATA[$P{SCHEMA_NAME}]]></expression>
      </parameter>
      <parameter name="contractorId">
        <expression><![CDATA[$P{contractorId}]]></expression>
      </parameter>
    </element>
  </band>
</detail>
```

**Паттерны:**
1. **textField элементы:**
   - `blankWhenNull="true"` - скрывать NULL значения
   - `expression` - выражение для вычисления значения
   - `$F{fieldName}` - ссылка на поле данных

2. **Подотчёты (subreport):**
   - `connectionExpression` - подключение к БД (передаётся из родительского отчёта)
   - `expression` - путь к скомпилированному .jasper файлу
   - `parameter` - передача параметров в подотчёт

3. **Условные выражения:**
   ```xml
   <expression><![CDATA[$F{ogName} != null ? $F{ogName} : ""]]></expression>
   ```
   - Java ternary operator для обработки NULL

**Рекомендации для нашего отчёта:**
- ✅ Использовать `blankWhenNull="true"` для всех полей
- ✅ Использовать условные выражения для вычисляемых полей
- ⚠️ Подотчёты не требуются (пока)

### 2.6 Форматирование элементов

#### 2.6.1 Статический текст с фоном

```xml
<element kind="staticText" 
         mode="Opaque" 
         x="0" y="40" width="80" height="30" 
         backcolor="#CCFFFF">
  <text><![CDATA[ogName]]></text>
</element>
```

**Паттерны:**
- `mode="Opaque"` - непрозрачный фон
- `backcolor="#CCFFFF"` - цвет фона (HEX формат)
- `mode="Transparent"` - прозрачный фон (по умолчанию)

#### 2.6.2 Текстовое поле с фоном

```xml
<element kind="textField" 
         mode="Opaque" 
         x="80" y="40" width="680" height="30" 
         backcolor="#CCFFFF" 
         blankWhenNull="true" 
         fontName="DejaVu Sans" 
         pdfFontName="DejaVu Sans">
  <expression><![CDATA[$F{ogName} != null ? $F{ogName} : ""]]></expression>
</element>
```

**Паттерны:**
- `fontName` - шрифт для экрана
- `pdfFontName` - шрифт для PDF (должен совпадать)
- `backcolor` - цвет фона элемента

**Рекомендации для нашего отчёта:**
- ✅ Использовать Times New Roman (как в Access отчёте)
- ✅ Использовать цвета из анализа Access отчёта:
  - RGB(253,234,218) → #FDEADA
  - RGB(252,213,181) → #FCD5B5
  - RGB(243,240,246) → #F3F0F6
  - RGB(198,217,241) → #C6D9F1

---

## 3. Анализ подотчёта (my-new-report-sr-ag.jrxml)

### 3.1 Настройки страницы

```xml
<jasperReport 
  name="my-new-report-sr-ag" 
  language="java" 
  columnCount="1" 
  pageWidth="760" 
  pageHeight="90" 
  orientation="Landscape" 
  columnWidth="750" 
  leftMargin="5" 
  rightMargin="5" 
  topMargin="5" 
  bottomMargin="5">
```

**Характеристики:**
- **Формат:** Кастомный (760 x 90 points) - под размер родительского элемента
- **Поля:** 5 points (минимальные)
- **columnCount:** 1 (одна колонка)
- **Ориентация:** Landscape

**Особенности подотчётов:**
- Размеры подотчёта должны соответствовать размерам элемента subreport в родительском отчёте
- Поля минимальные (5 points)
- Не используется pageHeader/pageFooter (только columnHeader и detail)

### 3.2 Параметры подотчёта

```xml
<parameter name="SCHEMA_NAME" class="java.lang.String">
  <defaultValueExpression><![CDATA[$P{SCHEMA_NAME}]]></defaultValueExpression>
</parameter>

<parameter name="contractorId" class="java.lang.String">
  <defaultValueExpression><![CDATA[$P{contractorId}]]></defaultValueExpression>
</parameter>
```

**Паттерны:**
- Параметры получают значения из родительского отчёта
- `defaultValueExpression` ссылается на параметр родителя: `$P{SCHEMA_NAME}`

### 3.3 SQL запрос подотчёта

```xml
<query language="SQL"><![CDATA[
SELECT 
  oga.ogaKey AS objKey,
  oga.ogaCode AS objName,
  og.ogNm AS organizationName
FROM $P!{SCHEMA_NAME}.ogAg oga
INNER JOIN $P!{SCHEMA_NAME}.og og ON oga.ogaOg = og.ogKey
WHERE oga.ogaOg = CAST($P{contractorId} AS INT)
]]></query>
```

**Паттерны:**
- JOIN для получения связанных данных
- Использование параметров из родительского отчёта
- Алиасы для читаемости

### 3.4 Структура bands подотчёта

#### 3.4.1 Column Header (Заголовки столбцов)

```xml
<columnHeader height="24" splitType="Stretch">
  <element kind="staticText" 
           uuid="16c5ffa2-3787-44c2-b3cb-85990d695e13" 
           x="0" y="0" width="80" height="18">
    <text><![CDATA[objKey]]></text>
  </element>
  <!-- ... другие заголовки ... -->
</columnHeader>
```

**Паттерны:**
- `columnHeader` используется для заголовков таблицы
- Высота: 24 points (≈8 mm)
- Элементы: `staticText` с названиями столбцов

#### 3.4.2 Detail (Строки данных)

```xml
<detail>
  <band height="19" splitType="Stretch">
    <element kind="textField" 
             uuid="aef5f9e6-9174-41f9-8df3-938e569efe74" 
             x="0" y="0" width="80" height="18">
      <expression><![CDATA[$F{objKey}]]></expression>
    </element>
    <!-- ... другие поля ... -->
  </band>
</detail>
```

**Паттерны:**
- Высота строки: 19 points (≈7 mm)
- `splitType="Stretch"` - растягивание при переносе
- Высота элементов: 18 points (чуть меньше высоты band)

**Рекомендации для нашего отчёта:**
- ✅ Использовать `columnHeader` для заголовков столбцов (27 контролов)
- ✅ Высота строки Detail: 55 points (19 mm) - как в Access отчёте
- ✅ Высота элементов: 9-27 points (в зависимости от контрола)

---

## 4. Анализ JSON metadata (my-new-report.json)

### 4.1 Структура metadata

```json
{
  "id": "my-new-report",
  "version": "1.0.0",
  "name": "Организация с кодом (кодами) агента",
  "description": "Карточка организации с вложенным списком кодов агента...",
  "category": "contractors",
  "author": "FEMSQ Team",
  "created": "2025-11-27",
  "lastModified": "2025-11-27",
  "files": {
    "template": "my-new-report.jrxml",
    "thumbnail": null
  },
  "parameters": [
    {
      "name": "contractorId",
      "type": "string",
      "label": "ID организации",
      "description": "Код организации (ogKey) для формирования отчёта",
      "required": true
    }
  ],
  "uiIntegration": {
    "showInReportsList": true,
    "contextMenus": [
      {
        "component": "OrganisationCard",
        "label": "Организация с кодом агента",
        "icon": "folder",
        "parameterMapping": {
          "contractorId": "${contractorId}"
        }
      }
    ]
  },
  "tags": ["contractors", "objects", "subreport", "demo"],
  "accessLevel": "user"
}
```

### 4.2 Ключевые поля

| Поле | Тип | Назначение | Пример |
|------|-----|------------|--------|
| `id` | string | Уникальный идентификатор | "my-new-report" |
| `version` | string | Версия отчёта | "1.0.0" |
| `name` | string | Отображаемое название | "Организация с кодом..." |
| `description` | string | Описание отчёта | "Карточка организации..." |
| `category` | string | Категория отчёта | "contractors" |
| `author` | string | Автор отчёта | "FEMSQ Team" |
| `created` | date | Дата создания | "2025-11-27" |
| `lastModified` | date | Дата изменения | "2025-11-27" |
| `files.template` | string | Путь к JRXML | "my-new-report.jrxml" |
| `files.thumbnail` | string\|null | Превью изображение | null |
| `parameters[]` | array | Параметры отчёта | см. ниже |
| `uiIntegration` | object | Интеграция с UI | см. ниже |
| `tags` | array | Теги для поиска | ["contractors", "objects"] |
| `accessLevel` | string | Уровень доступа | "user" |

### 4.3 Параметры

```json
{
  "name": "contractorId",
  "type": "string",
  "label": "ID организации",
  "description": "Код организации (ogKey) для формирования отчёта",
  "required": true
}
```

**Паттерны:**
- `name` - имя параметра (должно совпадать с JRXML)
- `type` - тип параметра (string, integer, date, boolean)
- `label` - отображаемое название
- `description` - описание параметра
- `required` - обязательность параметра

**Рекомендации для нашего отчёта:**
- ✅ Уже создан JSON metadata с правильной структурой
- ✅ Параметры: `ipgCh` (integer), `MounthEndDate` (date)
- ✅ Добавлены `pageSettings` для A3 Landscape

### 4.4 UI Integration

```json
{
  "uiIntegration": {
    "showInReportsList": true,
    "contextMenus": [
      {
        "component": "OrganisationCard",
        "label": "Организация с кодом агента",
        "icon": "folder",
        "parameterMapping": {
          "contractorId": "${contractorId}"
        }
      }
    ]
  }
}
```

**Паттерны:**
- `showInReportsList` - показывать в каталоге отчётов
- `contextMenus` - контекстные меню в компонентах UI
- `parameterMapping` - маппинг параметров из контекста

**Рекомендации для нашего отчёта:**
- ✅ `showInReportsList: true` (уже добавлено)
- ⚠️ `contextMenus` - пока не требуется (можно добавить позже)

---

## 5. Выводы и рекомендации для mstrgAg_23_Branch_q2m_2408_25

### 5.1 ✅ Уже реализовано правильно

1. **Параметры:**
   - ✅ `SCHEMA_NAME` - динамическая схема БД
   - ✅ Бизнес-параметры: `ipgCh`, `MounthEndDate`

2. **SQL запрос:**
   - ✅ Использование `$P!{SCHEMA_NAME}` для схемы
   - ✅ ORDER BY для сортировки

3. **Поля данных:**
   - ✅ Правильные типы (BigDecimal, Double, String)
   - ✅ Все 44 поля определены

4. **JSON metadata:**
   - ✅ Правильная структура
   - ✅ Параметры описаны
   - ✅ Категория "mstrg" создана

### 5.2 ⚠️ Требуется доработка

1. **Секция Title:**
   - ⚠️ Добавить заголовок отчёта (многострочный текст из Access)
   - ⚠️ Настроить шрифт: Times New Roman, size=8, bold

2. **Секция Page Header:**
   - ⚠️ Добавить 27 заголовков столбцов
   - ⚠️ Многоуровневая структура (3 уровня)
   - ⚠️ Цвета фонов: #FDEADA, #FCD5B5, #F3F0F6, #C6D9F1
   - ⚠️ Шрифт: Times New Roman, size=7, color=#7F7F7F

3. **Секция Detail:**
   - ⚠️ Добавить 42 поля данных
   - ⚠️ Высота строки: 55 points (19 mm)
   - ⚠️ Условное форматирование (цвета для разных типов данных)
   - ⚠️ Вычисляемые поля (ag_PlFulfillmentCn, mnCn и др.)

4. **Стили:**
   - ⚠️ Доработать 6 предопределённых стилей
   - ⚠️ Добавить условные стили для разных типов строк

5. **Группировка:**
   - ⚠️ Настроить группировку по `ipgSh` (если требуется визуальное разделение)

### 5.3 📋 Паттерны для применения

1. **Обработка NULL:**
   ```xml
   <textField blankWhenNull="true">
     <textFieldExpression><![CDATA[$F{ogNm}]]></textFieldExpression>
   </textField>
   ```

2. **Условные выражения:**
   ```xml
   <textFieldExpression><![CDATA[
     $F{ag_lim} == null ? null : $F{mn}
   ]]></textFieldExpression>
   ```

3. **Цвета фонов:**
   ```xml
   <textField mode="Opaque" backcolor="#FDEADA">
     <textFieldExpression><![CDATA[$F{ag_PlAccum}]]></textFieldExpression>
   </textField>
   ```

4. **Форматирование чисел:**
   ```xml
   <textField pattern="#,##0">
     <textFieldExpression><![CDATA[$F{ag_PlAccum}]]></textFieldExpression>
   </textField>
   ```

5. **Форматирование процентов:**
   ```xml
   <textField pattern="##0.0%">
     <textFieldExpression><![CDATA[$F{ag_PlPz}]]></textFieldExpression>
   </textField>
   ```

---

## 6. Сравнительная таблица

| Аспект | my-new-report | mstrgAg_23_Branch_q2m_2408_25 | Статус |
|--------|---------------|-------------------------------|--------|
| **Формат** | A4 Landscape | A3 Landscape | ✅ Настроено |
| **Поля** | 20 points | 14 points | ✅ Настроено |
| **Параметры** | 3 (SCHEMA_NAME, contractorId, SUBREPORT_DIR) | 3 (SCHEMA_NAME, ipgCh, MounthEndDate) | ✅ Настроено |
| **SQL запрос** | Простой SELECT | SELECT с ORDER BY | ✅ Настроено |
| **Fields** | 4 поля | 44 поля | ✅ Настроено |
| **Title** | Простой заголовок | Многострочный (TODO) | ⚠️ Требуется |
| **Page Header** | Нет | 27 заголовков (TODO) | ⚠️ Требуется |
| **Detail** | 3 поля + подотчёт | 42 поля (TODO) | ⚠️ Требуется |
| **Стили** | Нет | 6 стилей (частично) | ⚠️ Требуется |
| **Группировка** | Нет | По ipgSh | ✅ Настроено |
| **Подотчёты** | Да (1 подотчёт) | Нет | ✅ Не требуется |

---

## 7. Контрольные точки

### ✅ Этап 08.4 - ЗАВЕРШЁН

- ✅ Найдены существующие отчёты (2 примера)
- ✅ Изучена структура JRXML файлов:
  - ✅ Настройки страницы
  - ✅ Параметры
  - ✅ SQL запросы
  - ✅ Структура bands
  - ✅ Форматирование элементов
- ✅ Изучена структура JSON metadata
- ✅ Изучен пример подотчёта
- ✅ Создан документ анализа (этот файл)

### 📋 Следующие этапы

**09.0 - Создание базовой структуры JRXML:**
- 09.1: Реализация Report Header (Title) с многострочным заголовком
- 09.2: Реализация Page Header с 27 заголовками столбцов
- 09.3: Реализация Detail секции с 42 полями данных

---

**Файл создан:** 2025-12-05  
**Автор:** Александр  
**Статус:** Этап 08.4 завершён ✅  
**Общий прогресс:** Подготовка завершена на 100% (4/4 подэтапов) 🎉
