**Дата:** 2025-11-13  
**Автор:** Александр  
**Этап:** 02.4 (предложение)

# Предложение: Динамическое подключение к БД через UI

## Проблема

Текущая реализация:
- Frontend только имитирует подключение (демо-логика с `simulateErrorToggle`)
- Backend читает конфигурацию из файла `~/.femsq/database.properties` при старте
- Нет возможности изменить подключение к БД через UI без перезапуска backend
- ConnectionFactory создаётся как Spring Bean и не пересоздаётся при изменении конфигурации

## Требования

1. **Пользователь должен иметь возможность:**
   - Подключаться к любому источнику данных через UI
   - Менять схему/базу данных без перезапуска backend
   - Видеть текущее состояние подключения

2. **При тестировании:**
   - По умолчанию должно использоваться подключение к тестовой схеме (`ags_test`)
   - Конфигурация должна загружаться из файла при старте

## Предлагаемое решение

### Архитектура

```
Frontend (ConnectionModal)
    ↓ POST /api/v1/connection/test
Backend (ConnectionController)
    ↓ validate & test connection
    ↓ POST /api/v1/connection/apply
Backend (ConnectionController)
    ↓ saveConfig() + reconnect()
ConnectionFactory (переподключение)
    ↓ новые подключения используют новую конфигурацию
```

### Компоненты для реализации

#### 1. Backend: REST API для управления подключением

**Файл:** `femsq-web/src/main/java/com/femsq/web/api/rest/ConnectionController.java`

**Endpoints:**
- `GET /api/v1/connection/status` - получить текущее состояние подключения
- `POST /api/v1/connection/test` - проверить подключение (без сохранения)
- `POST /api/v1/connection/apply` - применить новую конфигурацию и переподключиться
- `GET /api/v1/connection/config` - получить текущую конфигурацию (без пароля)

**DTO:**
```java
public record ConnectionTestRequest(
    String host,
    Integer port,
    String database,
    String schema,
    String username,
    String password,
    String authMode
) {}

public record ConnectionStatusResponse(
    boolean connected,
    String schema,
    String database,
    String message,
    String error
) {}
```

#### 2. Backend: Механизм переподключения

**Вариант A: ConnectionManager с динамическим переподключением (рекомендуется для production)**

- Создать `ConnectionManager` как Spring Bean, который управляет `ConnectionFactory`
- `ConnectionFactory` остаётся singleton, но конфигурация обновляется динамически
- При изменении конфигурации:
  1. Сохранить новую конфигурацию в файл
  2. Закрыть все активные соединения в пуле (HikariCP)
  3. Обновить конфигурацию в `ConnectionFactory` (через `reloadConfig()`)
  4. Новые соединения будут использовать новую конфигурацию
  5. DAO бины не требуют пересоздания (используют обновлённый `ConnectionFactory`)

**Вариант B: Пересоздание ConnectionFactory**

- Сделать `ConnectionFactory` не singleton, а prototype scope
- При изменении конфигурации:
  1. Сохранить новую конфигурацию в файл
  2. Закрыть старый ConnectionFactory
  3. Создать новый ConnectionFactory с новой конфигурацией
  4. Обновить все DAO бины через `ApplicationContext`

**Вариант C: In-memory конфигурация (для production без файла)**

- Хранить конфигурацию только в памяти
- Не сохранять в файл при изменении через UI
- При перезапуске использовать значения по умолчанию или переменные окружения

**Рекомендация:** 
- **Для development и production:** Вариант A (ConnectionManager) - наиболее универсальный

**Детали реализации ConnectionManager:**

```java
@Service
public class ConnectionManager {
    private final DatabaseConfigurationService configService;
    private final HikariDataSource dataSource; // или JdbcConnector
    private volatile DatabaseConfigurationProperties currentConfig;
    
    @PostConstruct
    public void init() {
        currentConfig = configService.loadConfig();
        // Инициализация пула соединений
    }
    
    public void reconnect(DatabaseConfigurationProperties newConfig) {
        // 1. Валидация новой конфигурации
        // 2. Тестирование подключения
        // 3. Закрытие старых соединений
        // 4. Обновление конфигурации
        // 5. Пересоздание пула соединений
        // 6. Сохранение в файл (если требуется)
    }
}
```

**Преимущества:**
- Работает одинаково в dev и production
- Не требует пересоздания Spring бинов
- Можно управлять пулом соединений (HikariCP)
- Поддерживает graceful shutdown старых соединений

#### 3. Backend: Поддержка схемы в конфигурации

**Расширить `DatabaseConfigurationProperties`:**
```java
public record DatabaseConfigurationProperties(
    String host,
    Integer port,
    String database,
    String schema,  // ← добавить
    String username,
    String password,
    String authMode
) {}
```

**Обновить DAO для использования схемы:**
- `JdbcOgDao` и `JdbcOgAgDao` должны использовать схему из конфигурации
- SQL запросы: `SELECT * FROM ${schema}.og` вместо `ags_test.og`

#### 4. Frontend: Реальное подключение через API

**Заменить демо-логику в `App.vue`:**

```typescript
async function handleSubmit(values: ConnectionFormValues): Promise<void> {
  modalStatus.value = 'validating';
  modalMessage.value = 'Проверяем данные…';
  modalError.value = '';

  try {
    // 1. Тестируем подключение
    const testResponse = await apiPost('/api/v1/connection/test', {
      host: values.host,
      port: parseInt(values.port),
      database: values.database,
      schema: values.schema,
      username: values.username,
      password: values.password,
      authMode: values.authMode
    });

    if (!testResponse.success) {
      modalStatus.value = 'error';
      modalError.value = testResponse.error || 'Не удалось подключиться';
      return;
    }

    // 2. Применяем конфигурацию
    modalStatus.value = 'connecting';
    modalMessage.value = 'Применяем конфигурацию…';
    
    await apiPost('/api/v1/connection/apply', {
      host: values.host,
      port: parseInt(values.port),
      database: values.database,
      schema: values.schema,
      username: values.username,
      password: values.password,
      authMode: values.authMode
    });

    // 3. Обновляем состояние
    connection.setStatus('connected', {
      schema: values.schema,
      user: values.username || 'sa',
      message: `Подключено к ${values.schema || values.database}`
    });
    connection.updateSavedForm(values);
    
    // 4. Загружаем данные
    await organizationsStore.fetchOrganizations();
    
    modalStatus.value = 'success';
    modalMessage.value = 'Подключено';
    
    setTimeout(() => {
      modalOpen.value = false;
      connection.navigate('organizations');
    }, 500);

  } catch (error) {
    modalStatus.value = 'error';
    modalError.value = error instanceof Error ? error.message : 'Ошибка подключения';
    connection.setStatus('connectionError', {
      error: modalError.value,
      message: 'Попробуйте снова позже'
    });
  }
}
```

#### 5. Frontend: API клиент для подключения

**Добавить в `src/api/http.ts`:**
```typescript
export async function apiPost<T>(path: string, data: unknown, options?: RequestOptions): Promise<T> {
  // Реализация POST запроса
}
```

### План реализации

#### Этап 1: Backend API (приоритет: высокий)
1. ⏳ Создать `ConnectionController` с endpoints
2. ⏳ Добавить DTO для запросов/ответов
3. ⏳ Реализовать тестирование подключения (без сохранения)
4. ⏳ Реализовать применение конфигурации
5. ⏳ Добавить поддержку схемы в конфигурацию
6. ⏳ Реализовать механизм переподключения (ConnectionManager)
7. ⏳ Добавить поддержку внешнего пути к конфигурации (для production)

#### Этап 2: Обновление DAO (приоритет: высокий)
1. ⏳ Обновить `JdbcOgDao` для использования схемы из конфигурации
2. ⏳ Обновить `JdbcOgAgDao` для использования схемы из конфигурации
3. ⏳ Протестировать работу с разными схемами

#### Этап 3: Frontend интеграция (приоритет: высокий)
1. ⏳ Добавить `apiPost` в HTTP клиент
2. ⏳ Заменить демо-логику на реальные API вызовы
3. ⏳ Добавить обработку ошибок подключения
4. ⏳ Обновить UI для отображения реального статуса

#### Этап 4: Тестирование (приоритет: средний)
1. ⏳ Unit тесты для `ConnectionController`
2. ⏳ Integration тесты для переподключения
3. ⏳ E2E тесты для смены схемы через UI

### Конфигурация по умолчанию для тестирования

**При старте backend:**
- Если файл `~/.femsq/database.properties` отсутствует, создать с параметрами:
  - `host=localhost`
  - `port=1433`
  - `database=FishEye`
  - `schema=ags_test` ← по умолчанию тестовая схема
  - `username=sa`
  - `password=` (пустой, пользователь введёт)
  - `authMode=credentials`

**Или через переменные окружения:**
```bash
FEMSQ_DB_HOST=localhost
FEMSQ_DB_PORT=1433
FEMSQ_DB_NAME=FishEye
FEMSQ_DB_SCHEMA=ags_test  # ← по умолчанию
FEMSQ_DB_USER=sa
FEMSQ_DB_PASSWORD=...
```

### Альтернативные подходы

#### Подход 1: Session-based конфигурация
- Хранить конфигурацию в сессии пользователя
- Каждый пользователь может иметь свою конфигурацию
- Требует поддержки сессий (Spring Session)

#### Подход 2: Multi-tenant архитектура
- Поддержка нескольких подключений одновременно
- Переключение между подключениями через контекст
- Более сложная реализация

**Рекомендация:** Начать с простого подхода (одно подключение на backend), затем при необходимости расширить.

### Оценка сложности

- **Backend API:** 4-6 часов
- **Механизм переподключения:** 6-8 часов
- **Обновление DAO:** 2-3 часа
- **Frontend интеграция:** 3-4 часа
- **Тестирование:** 2-3 часа

**Итого:** ~17-24 часа работы

### Совместимость с production (JAR deployment)

#### ✅ Работает корректно:

1. **Запись конфигурации:**
   - Файл `~/.femsq/database.properties` находится вне JAR (в домашней директории пользователя)
   - `System.getProperty("user.home")` работает одинаково в dev и production
   - Запись в файл не зависит от того, запущено ли приложение из JAR

2. **Чтение конфигурации:**
   - `Files.newInputStream()` и `Files.newOutputStream()` работают с файлами вне JAR
   - Нет проблем с доступом к файлам в production

3. **Кроссплатформенность (Linux/Windows):**
   - `System.getProperty("user.home")` работает в Windows (`C:\Users\Username`) и Linux (`/home/username`)
   - `Paths.get()` автоматически обрабатывает разделители путей (`\` в Windows, `/` в Linux)
   - POSIX права обрабатываются через `try-catch UnsupportedOperationException` (Windows не поддерживает POSIX)
   - В Windows права доступа контролируются стандартными механизмами Windows (ACL)

#### ⚠️ Требует внимания:

1. **Пересоздание Spring бинов:**
   - В production пересоздание `ConnectionFactory` и DAO может быть проблематично
   - **Решение:** Использовать `ApplicationContext` для обновления бинов или создать `ConnectionManager` с динамическим переподключением

2. **Множественные экземпляры:**
   - Если запущено несколько экземпляров JAR, каждый будет использовать свой файл конфигурации
   - **Решение:** Использовать общий файл конфигурации или синхронизацию через внешний сервис

3. **Права доступа:**
   - В production нужно убедиться, что пользователь имеет права на запись в `~/.femsq/`
   - **Решение:** Проверять права при старте и создавать директорию с правильными правами
   - **Windows:** Права контролируются через ACL (Access Control List), код автоматически обрабатывает отсутствие POSIX прав
   - **Linux:** Используются POSIX права (`rwx------` для директории, `rw-------` для файла)

#### 🔧 Рекомендации для production:

1. **Использовать внешний конфигурационный файл:**
   ```bash
   # Linux
   java -jar femsq-{version}.jar \
     -Dfemsq.config.path=/etc/femsq/database.properties
   
   # Windows
   java -jar femsq-{version}.jar \
     -Dfemsq.config.path=C:\ProgramData\FEMSQ\database.properties
   ```

2. **Поддержка переменных окружения:**
   - Позволить переопределять путь к конфигурации через переменную окружения
   - **Linux:** `FEMSQ_CONFIG_PATH=/etc/femsq/database.properties`
   - **Windows:** `FEMSQ_CONFIG_PATH=C:\ProgramData\FEMSQ\database.properties`

3. **Альтернативный подход для production:**
   - Хранить конфигурацию в памяти (не сохранять в файл)
   - При перезапуске использовать значения по умолчанию или переменные окружения
   - Или использовать внешний конфигурационный сервер

4. **Проверка кроссплатформенности:**
   - Текущая реализация `ConfigurationFileManager` уже поддерживает Windows:
     - `System.getProperty("user.home")` → `C:\Users\Username` в Windows
     - `Paths.get()` автоматически обрабатывает разделители
     - POSIX права обрабатываются через `try-catch` (Windows выбрасывает `UnsupportedOperationException`)
   - **Пути в Windows:**
     - По умолчанию: `C:\Users\Username\.femsq\database.properties`
     - Можно переопределить через `-Dfemsq.config.path` или переменную окружения
   - **Текущая реализация уже поддерживает Windows:**
     - `ConfigurationFileManager.writeProperties()` обрабатывает `UnsupportedOperationException` при создании файла с POSIX правами
     - В Windows файл создаётся без POSIX атрибутов, права контролируются через ACL
     - `applyDirectoryPermissions()` также обрабатывает отсутствие POSIX прав в Windows

### Риски

1. **Переподключение может быть медленным** - нужно закрыть все активные соединения
2. **Потеря данных при переподключении** - активные транзакции могут быть прерваны
3. **Безопасность** - пароли передаются через API (использовать HTTPS в production)
4. **Production deployment** - пересоздание Spring бинов может потребовать дополнительной реализации

### Следующие шаги

1. Обсудить подход к переподключению (Вариант A vs B)
2. Создать задачу в `project-development.json`
3. Начать с Backend API (этап 1)
4. Затем обновить DAO (этап 2)
5. Интегрировать с Frontend (этап 3)

---

**Статус:** Предложение  
**Приоритет:** Высокий  
**Связанные задачи:** task:0018 (UI подключения)
